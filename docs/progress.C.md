# progress.C.md — Lane C: Quality, PII & Interface

> Lane file. Owner: **Eng C — Quality, PII & Interface** (PII boundary, CLI/interface, eval/quality).
> Global facts (build phase, contracts, decisions) live in the index `docs/progress.md` — don't duplicate them here.
> Headers below are stable so `/handoff` can target them. Append to the session log, newest first; rewrite the other sections from current state.

## In flight
- _(none)_

## Next up
1. **Merge `feat/c/003-pseudonymised-lm-boundary` → `main`**, then `/handoff-index` (the shared index still says boundary stubbed / 4 contracts / AD-≤096 — all now stale). The branch is review-clean and GREEN.
2. **AD-068 vault hardening** (follow-on to AD-098) — encrypt the `FileVault` at rest + retention limits + purge-by-`candidate_id`. The store is currently plaintext (gitignored) by design; this is the deferred hardening. Needs a spec.
3. **AD-084 generic outbound NER/org-dictionary scan** on the index/embed path — the adversarial review confirmed third-party names in gold `projects` can reach Modal's embedder unredacted (the `build_embed_text` seam takes no `known_pii`). Out of scope for c-003 (query-score boundary only); this is the real residual-PII hardening for the write/index path. Needs a spec.
4. **Narrative-faithfulness LLM judge** — G-Eval judge for narrative quality. Deferred from c-002: needs labelled validation data + TPR/TNR ≥ 80% validation before wiring.
5. **Retrieval-quality metrics** (Recall@K, contextual precision/recall) — meaningful now that hybrid recall is ON (AD-089).

## Blockers / needs a human
- _(none)_ — but note the **AD-084 embed-path leak** (Next up #3) is a real, tracked residual-PII gap on the *index* path, deliberately deferred (not introduced by c-003). Don't let the index path be described as fully PII-clean until it lands.

## Session log (append-only — newest first)
- **2026-06-23 · c-003-pseudonymised-lm-boundary** — Implemented T-000-ADR…T-007 + review hardening. Ratified **AD-097** (real PseudonymisedLM query-time boundary) + **AD-098** (minimal persistent `FileVault` + `get_identity`; plaintext, encryption deferred to AD-068). Closed the real query-time leak: the serving `Candidate`'s `feedback`/`profile_summary` carry **de-anonymised** gold free-text, and `score` sent them through the no-op stub. Now: `PseudonymisedLM.__call__` redacts prompt+messages under one `Redactor` (deterministic vault-backed strip + NER residual), `assert_no_leak` hard-gates each fragment, forwards, then de-anonymises the response; **unset `pii_context` = pass-through** (clarify, §7). New `Redactor`/`redact_fragments` give one stable cross-fragment mapping (`dsm/pii/redact.py`). `FileVault` (`dsm/pii/vault.py`) persists `cid→(name,email)` (gitignored `data/identity/`, atomic write); ingest **writes** it (`--vault-path`, defaults beside gold), query **reads** it. Wiring lives at the CLI (`_pii_aware_score_predictor` resolves the vault by `candidate_id` + enters `pii_context`) so **`dsm.match` stays PII-free** — enforced by a **new 5th import contract** (`Match ⊥ PII`). `no-PII-leak` eval invariant upgraded from structural-only → exercises the real anonymiser (planted-name Tier-1 test + deliberately-failing fixture); Tier-3 live smoke drives the full vault→pii_context→PseudonymisedLM path. Ran an adversarial review workflow (29 agents): 7 findings confirmed, 17 refuted — fixed 5 (case-sensitive NER round-trip, `deanonymize` fixpoint for nested placeholders, non-str guard, `FileVault` type-validated load + atomic flush); the AD-084 embed-path third-party-name leak is real but deferred (Next up #3). `make check` **GREEN** (419 unit + 19 Tier-1, 5 contracts); `make eval` GREEN (Tier-1+2; Tier-3 skips without keys). Branch: `feat/c/003-pseudonymised-lm-boundary`. Next: merge + `/handoff-index`.
- **2026-06-23 · c-002-query-eval-harness** — Implemented T-000 through T-010: ratified AD-093 (three-tier eval architecture, drop promptfoo) + AD-094 (marker scheme + collection rules). Built six pure invariant evaluators in `dsm/eval/invariants.py` (gates-respected, hard-skill-not-cleared-by-adjacency, evidence-cited, no-PII-leak, determinism, adjacency-flag). Created golden-case + cassette-LM framework in `dsm/eval/cases.py` with `pseudonymise_candidates` mirroring `GoldCandidateStore`. Hand-authored cassette JSON fixtures for ROLE-01/02/03 under `tests/fixtures/cassettes/`. Enriched seed fixtures with `profile_summary` and `FeedbackEntry` items; added Suresh (java, HARD_SKILL_MISMATCH) + `desired_skills=[java/DESIRED]` to ROLE-01 for adjacency testing. Tier-1 runner: 17 tests (all six invariants × golden cases + deliberately-failing tampered fixtures + provider guard proof). Tier-2 signature regression: 7 tests (cassette freshness + clarify/score shape pins). Tier-3 live smoke + drift guard: 2 tests (skipif no keys). Wired `make eval` (no longer stubs), `make eval-tier1` (Tier-1 in `make check`), `make eval-record` (cassette regeneration). Updated eval README. `make check` GREEN (387 unit + 17 Tier-1 eval, 4 contracts). `make eval` GREEN (26 tests). Branch: `feat/c/002-query-eval-harness`. Next: merge to main, then real `PseudonymisedLM` boundary.
- **2026-06-16 · c-001-gates-rank** — Implemented T-001…T-007: real location + availability gates (`dsm/match/gates.py`; AD-020/021/022, AD-063a; shared `effective_free_date`), config-free rank sort/tie-break/top-k (`dsm/match/rank.py`; AD-043), orchestrator no-match path + `build_near_misses` (`dsm/cli/commands.py`; AD-063b/c/d, gaps recomputed from structured data not `detail`), and importable ROLE-01/02/03 fixtures (`tests/fixtures/`). Added `dsm/config.py` YAML loader + declared PyYAML, recorded as **AD-064** (orchestrator owns config + `config_snapshot`; rank stays config-free) — confirmed with the human at the post-T-003 checkpoint before adding the dep. All 24 EARS criteria covered by tests; adversarial review clean; closed the design's empty-candidate-list edge case. `make check` GREEN (66 tests, 2 import contracts; `gates.py` import-clean). Next: merge + `/handoff-index`, then wire `make eval` on the seed fixtures.
- **2026-06-14 · lane-split** — Split shared `progress.md` into per-lane files; seeded this Lane C file from the Quality/PII/Interface slices. `make check` GREEN (29 tests, 2 import contracts). Next: wire up `make eval`.
