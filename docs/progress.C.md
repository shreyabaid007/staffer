# progress.C.md — Lane C: Quality, PII & Interface

> Lane file. Owner: **Eng C — Quality, PII & Interface** (PII boundary, CLI/interface, eval/quality).
> Global facts (build phase, contracts, decisions) live in the index `docs/progress.md` — don't duplicate them here.
> Headers below are stable so `/handoff` can target them. Append to the session log, newest first; rewrite the other sections from current state.

## In flight
- **c-011 — Web supply ingest — implemented on `feat/c/011-web-supply-ingest`, NOT yet merged; `make check` GREEN.** The supply pool is now managed from the web page and the pipeline runs incrementally behind one button. **(1) Ingest incrementality fix (AD-XXX):** two real defects closed — *G-1 enrichment loss*: `merge_run` only saw newly-landed blobs, so editing one supply CSV overwrote every candidate on that sheet with thin gold (no profile/feedback); the normalize→merge stages now process the **full current corpus** (LANDED + SKIPPED via persisted-bronze read-back, Docling never re-runs); *G-2 no enrich cache*: new `FileEnrichCache` (`dsm/ingest/enrich_cache.py`) keyed `sha256(candidate_id · source_hash · raw_text-hash · prompt_version · model_version)` under gitignored `data/enrich_cache/` — an unchanged doc makes **zero LLM calls**; a version bump re-extracts (§11 now enforced); plus a **gold write gate** (skip byte-stable entities; summary reports `updated/unchanged`) and `enrich : llm_calls/cache_hits` summary lines. Reconcile/tombstone/revive (AD-093/094), leak abort, quality gate all preserved. **(2) Web supply API (AD-XXY, `dsm/web/`):** `GET /supply` (three sheets from the raw CSVs + gold/silver sync status; `commands.derive_candidate_id` seam keeps `dsm.web ⊥ dsm.pii`), add/remove rows (sheet-schema-preserving, banner bumped, atomic, 409 dup), resume PDF upload/delete with pre-ingest `link_check`, feedback write-Markdown/upload/delete with guaranteed-link assembly (verified through the real `parse_markdown`; append-only), and `POST /ingest/run` + `GET /ingest/status` — a single-flight background job running `dsm ingest` → `dsm index` as subprocesses with the PII-safe summary parsed into a structured view. **(3) Frontend:** two-tab page — *Supply & Ingest* (grouped one-table view, inline add row, per-row resume/feedback actions, sync chips, Run-ingest button + polled incremental summary) + the c-008 match UI unchanged. **Verified live on real data (35 consultants):** warm run 93 llm_calls/0 hits then gold `updated=0 unchanged=35`; add-candidate+feedback run → `llm_calls=1 cache_hits=93 indexed=11 skipped=25`; removal run → `llm_calls=0 tombstoned=1 index_removed=1`. Frozen contract untouched; no `contract-snapshot`. **AD-XXX/AD-XXY placeholders** — assign at merge.
- _Prior, merged:_ **c-010 — Production-grade AI eval hardening — merged to `main` (PR #42, AD-115)** and **DSPy modernisation (PR #43, AD-116/117/118)** — see the index. Three **additive** `make eval` capabilities closing the top 2026-best-practice gaps for a people-ranking RAG system, reusing the AI-eval-layer patterns (signed-off corpora + the `validate_judge` confusion-matrix pattern). **(1) Guardrail-detector validation + calibration** (`dsm/eval/guardrail_validation.py`) — scores the c-009 jailbreak/bias/toxicity detectors as classifiers (precision/recall/F1/TPR/TNR + **Cohen's κ**) with a threshold sweep → F1/Youden's-J operating point. **(2) Prompt-injection red-team** (`dsm/eval/red_team.py`, OWASP LLM01) — a signed-off payload corpus through the **guarded** pipeline yields **ASR = 0** (rejected before the LLM call, never ranked) + no narrative leak, plus a live real-LLM susceptibility probe. **(3) Counterfactual fairness** (`dsm/eval/fairness.py`) — a demographic-proxy swap leaves the **deterministic** layer byte-identical (offline proxy-blindness) and the **real** score LLM's sub-scores within tolerance (live), backing the `product.md` fairness promise. Each has a deterministic offline tier (always runs) + a live tier (`eval_live`, skip-gated on keys / the `guardrails` extra). Eval modules are **pure** (no `dsm.match`/`dsm.pii`/`dsm.guardrails` import at load — tests wire them); corpora signed-off-gated like the golden set. Added Cohen's κ to detector reports. **Deterministic spine, frozen contract (AD-060), gates (AD-002), PII boundary, and the `make check` commit gate are all unchanged** — everything under `make eval`. **AD-XXX** placeholder. `make check` GREEN (570 passed, 20 Tier-1, 7 contracts — **unchanged scope**); `make eval` GREEN (80 passed, 4 skipped — the 3 guardrail-detector live tests + the c-009 live suite skip w/o the extra; the live fairness + live injection tiers **ran against the real LLM and passed**). **Recommended follow-ups (in the review, deferred):** the faithfulness judge is self-family (self-preference bias) → move to a non-self-family / PoLL panel; group four-fifths adverse-impact reporting (needs real data); grounding-detector validation (needs paired context); automated red-team tooling (garak/PyRIT).
- _Prior, merged:_ **c-009 — Guardrails AI (input hardening + output validation) — merged to `main` (PR #40, AD-114).** A complementary defence-in-depth content-safety layer (`dsm/guardrails/`) around the `PseudonymisedLM` call sites: `input_guard` (`detect_jailbreak`, before redaction + the LLM call), `output_guard` (score-bounds clamp + `bespoke_minicheck` grounding — the **remote** grounding call is fed PII-redacted + leak-scanned text), `narrative_guard` (`bias_check` + `toxic_language`, withhold-on-detection / fail-open-on-error). Wired at the CLI (`dsm.match ⊥ dsm.guardrails`); optional eval-tier extra (guards no-op when absent → `make check` hermetic). Frozen contract untouched.
- _Prior, merged:_ **c-008 — web frontend (thin FastAPI JSON API + one static page) — merged to `main` (PR #38, AD-113).** A minimal web UI over the **same** spine: a `dsm/web/` composition root (peer of `dsm/cli/`) exposing `/intake`→`/match/query` (NL door, two-step parse→confirm), `/demand/parse`→`/match/role` (CSV-upload door), `/resume/{candidate_id}` (original PDF), `/decisions` (append-only capture), `/` (the page), `/healthz`; plus `dsm serve` (uvicorn). PII boundary intact (AST-tested); frozen `dsm/models.py` untouched.
- _Prior, merged:_ **c-007 — query-side negation (`exclude_cities`, workshop A2) is merged to `main`** (PR #36, **AD-112**). Parses a location negation ("not <city>", "anywhere but <city>") into a typed `exclude_cities` set enforced by the **pure-Python location gate** — never the embedding (the A2 invariant). **Frozen-contract amendment** (AD-060): `exclude_cities: frozenset[str] = frozenset()` on `OpenRole` + `TargetProfileScorecard` (`make contract-snapshot` regenerated). The gate excludes a candidate whose **home city** is in the set (first, regardless of co-location); `build_near_misses` **skips** excluded candidates (an exclusion is non-negotiable — never a near-miss); **"anywhere but X"** (no positive city) assembles as a **distributed** role, not a missing-location clarification. The LLM only *proposes* the set (an LLM-parsed fact, echo-confirmed) — eligibility stays deterministic (AD-002); not the c-006 co-location blocker. Prompt generalised (no hardcoded city) + verified live ("not Chennai" and "not Bengaluru" both work). Scope: cities only — the general schema-driven filter (sectors/skills/candidate-level) is a deferred follow-up (see [`docs/backlog.md`](backlog.md)). **With c-006, this completes Part A** (A1+A3+A2; A4 relaxation stays deferred — covered by near-misses).
- _Prior:_ **c-006 — natural-language query intake (`dsm match --query`) is merged to `main`** (PR #34, **AD-110** front door + **AD-111** relative-date/NL-freshness; were AD-110/AD-111 placeholders on the branch). A prose front door parses free text → the **existing** `OpenRole` via a single-shot DSPy `Predict` over `PseudonymisedLM` (pass-through, temp 0 pinned), echoes the parse for confirmation, then runs the unchanged spine. Relative dates are LLM-resolved (today injected) + **Python-validated** before the availability gate; `co_location_required` is **Python-derived**, never an LLM output (AD-002/FR-8 — the blocker fix); NL **replaces** clarify's LLM with the deterministic echo path, so exactly one LLM interpretation runs; `demand_as_of`=run-date (freshness `ok`/`refuse` only). Content-hash parse cache + pinned `(model, prompt_version)` derivation (AD-066). **No frozen-contract change** (`RoleIntake` is match-local; produces `OpenRole`). Includes the NL-intake parse-quality eval (T-005, `make eval`). Prior Lane-C work merged: c-002 (PR #23), c-003 (PR #25, AD-101/102), c-004 + calibration (PR #27/#28, AD-104/105/106), AD-103. **Next: A2 query-side negation (`exclude_cities`)** — see [`docs/backlog.md`](backlog.md); then c-005 (AD-084). Read the index `docs/progress.md` for the merged build state.

## Next up
0. **c-011 — review + merge.** Adversarial review of the web-supply-ingest slice, open the PR, `/handoff-index` assigns the real numbers for **AD-XXX** (incremental ingest) + **AD-XXY** (web supply API). At merge, add the deferred-work note to `docs/backlog.md`: *provenance-hash re-embed bound* — editing a supply CSV re-embeds that sheet's candidates (blob hash → gold provenance → gold_hash) even when row content is unchanged; fixing it needs a provenance-excluding content hash (merge redesign). Also consider: job-runner concurrency with a live match request (Milvus Lite single-writer — job fails cleanly, documented NF-4), and resume delete only removes the web-conventional `<email-local>.pdf` (legacy name-based PDFs need manual removal).
1. **c-010 live-validator follow-up.** To run the live guardrail-detector validation once: install the `guardrails` extra + hub validators (as in the c-009 note below), then `tests/eval/test_guardrail_validation.py::TestLiveDetectorValidation` reports per-detector P/R/F1/κ + a calibration sweep instead of skipping.
   - **c-010 tracked follow-ups (in AD-XXX / the review, deferred):** (a) the faithfulness judge is **self-family** (Claude judging Claude → self-preference bias) → move to a non-self-family judge or a **PoLL** panel + report Cohen's κ; (b) **group four-fifths** adverse-impact reporting once real population data exists (the counterfactual is the pre-data guard); (c) **grounding-detector** validation (needs paired context); (d) automated red-team tooling (garak/PyRIT/DeepTeam) if the curated corpus proves insufficient.
   - **c-009 follow-ups (still open, see AD-114):** `INPUT_REJECTED` `ExclusionReason` (frozen-contract amendment); DSPy `REASK` for bias/toxicity through `PseudonymisedLM` (currently withhold-and-log); optionally re-instate `guardrails/valid_range`. Exercise the c-009 live validators once via `pip install 'staffer[guardrails]'` + `guardrails configure` + `guardrails hub install hub://guardrails/{detect_jailbreak,bias_check,toxic_language}` + `hub://bespokelabs/bespoke_minicheck` (`BESPOKE_API_KEY`).
2. **c-005 — AD-084 generic outbound NER/org-dictionary scan (PII, next slice).** Close the one remaining real leak: third-party names in gold `projects` reach Modal's embedder unredacted (`build_embed_text` takes no `known_pii`). This is the query-score boundary's index-path counterpart. **Write the spec (`requirements → design → tasks`) and stop for sign-off** (golden rule 1).
3. **AD-068 vault hardening** — encrypt the `FileVault` at rest + retention limits + purge-by-`candidate_id`. Plaintext (gitignored) by design now. Needs a spec.
4. **Lower-severity review findings 4–10** (cleanup, not boundary-critical): `_default_ner` word-boundary `str.replace`; O(N²) per-candidate `FileVault._flush`; `_outbound_text`/`_redact_messages` duplication; `redact_fragments` dead code (prod uses `_redact_messages`); the contextvar opt-in footgun; concurrent temp-file race in `_flush`; duplicated vault-path expression in `_match_role`/`ingest`. Triage into a follow-up (some fold naturally into AD-084).

## Blockers / needs a human
- _(none)_
- Note: the **AD-084 embed-path leak** (Next up #1) is a real, tracked residual-PII gap on the *index* path until c-005 lands. Don't describe the index path as fully PII-clean before then.

## Session log (append-only — newest first)
- **2026-07-02 · c-011 Web supply ingest (AD-XXX/AD-XXY) — GREEN** — New Lane-C slice on `feat/c/011-web-supply-ingest` (spec `specs/c-011-web-supply-ingest/` written first; ran straight to a working slice per the user's "implement now" directive — the c-008/c-009 precedent). The ask: manage beach/rolling-off/new-joiner candidates from the web page (inline table add/remove, per-row resume + feedback in Markdown-or-file), one button to trigger ingest, and **verify the pipeline is truly incremental — "if not fix that too"**. Orientation found it wasn't: **(G-1)** `merge_run` merged only newly-landed blobs, so a partial supply-CSV edit silently overwrote every candidate on that sheet with thin gold (enrichment dropped); **(G-2)** every normalized resume/feedback re-called the LLM even when unchanged (§11's "bump forces re-extraction" implied but unenforced). Fixed both (AD-XXX): full-corpus normalize→merge (SKIPPED blobs read persisted bronze via new `has_records`/`read_records` — no Docling re-parse), `FileEnrichCache` wired guard→cache→LLM at the composition root, gold write gate + `updated/unchanged` + `llm_calls/cache_hits` summary lines; known-PII coverage on partial runs also restored (previously only newly-landed sheets fed the redaction list). Web layer (AD-XXY): `dsm/web/supply.py` (sheet CRUD + attachments; raw CSVs stay the single source of truth; `commands.derive_candidate_id` seam keeps `dsm.web ⊥ dsm.pii`), `dsm/web/jobs.py` (single-flight background `dsm ingest`→`dsm index` subprocess job + summary parser), 10 new routes, and the two-tab frontend (supply table per the approved mockup + the c-008 match UI unchanged). Tests: cache unit tests, a **two-run CLI scenario** (edit sheet → 0 LLM calls, gold keeps enrichment, tombstone+revive, byte-stable third run, prompt-bump invalidation), 25 web tests (CRUD/attachments/job; feedback round-trips through the real `parse_markdown`); fixed a route-ordering collision (`{category}/{email}` shadowed `{email}/resume`) and a config-cache test-pollution bug (lru_cached dict mutated by the bump test). **Verified live** (`dsm serve`, real 35-consultant pool): warm run `llm_calls=93 cache_hits=0` → gold `updated=0 unchanged=35` (write gate byte-stable); web add candidate+feedback → run `llm_calls=1 cache_hits=93 gold_updated=11 indexed=11 skipped=25`; web remove → run `llm_calls=0 tombstoned=1 index_removed=1 skipped=25`. `data/enrich_cache/` + `data/ingest_jobs/` gitignored (PII-dense, derived). `make check` **GREEN** (603 passed, 20 Tier-1, 9 contracts). Frozen contract untouched. 8 commits on the branch. Next: review + merge → `/handoff-index` assigns the real AD numbers + backlog note (provenance-hash re-embed bound).
- **2026-07-01 · c-010 Production-grade AI eval hardening (AD-XXX)** — After merging c-009 (PR #40) + the index refresh (PR #41, which assigned **AD-114**), reviewed our eval framework against 2026 enterprise LLM/RAG best practice (via a research fan-out: RAG triad/RAGAS; LLM-judge validation incl. Cohen's κ + judge biases — flagging our judge is **self-family**; OWASP LLM Top-10 2025 red-team + ASR; guardrail detectors validated as classifiers; counterfactual fairness + four-fifths; F1/Youden's-J calibration; NIST AI RMF). Gap-checked: we already had deterministic invariants + validated faithfulness judge (AD-105) + retrieval metrics (AD-106); the three highest-value gaps for a **people-ranking** system were unvalidated guardrail *detectors*, no injection red-team, no fairness test. Implemented three additive `make eval` capabilities (spec `specs/c-010-eval-hardening/`): `dsm/eval/guardrail_validation.py` (detector P/R/F1/TPR/TNR + **Cohen's κ** + threshold sweep → F1/Youden's-J), `dsm/eval/red_team.py` (OWASP LLM01 injection corpus → **ASR=0** through the guarded pipeline + narrative-leak check + live susceptibility probe), `dsm/eval/fairness.py` (counterfactual: deterministic proxy-blindness + live sub-score parity), `dsm/eval/hardening_fixtures.py` (signed-off-gated loaders) + two signed-off corpora + three test tiers. Each = deterministic offline tier (always runs) + skip-gated live tier. Eval modules **pure** (tests wire the pipeline/guards). **Nothing touches the deterministic spine, frozen `dsm/models.py`, gates, the PII boundary, or the `make check` commit gate** — all under `make eval`. `make check` **GREEN** (570 passed, 20 Tier-1, 7 contracts — **unchanged scope**); `make eval` **GREEN** (80 passed, 4 skipped — guardrail-detector live + c-009 live suite skip w/o the extra; the live fairness + live injection tiers **ran against the real LLM and passed**). No `contract-snapshot`. Branch `feat/c/010-eval-hardening`. Next: review + merge → `/handoff-index`. Recommended follow-ups recorded (self-family→PoLL judge, four-fifths on real data, grounding validation).
- **2026-07-01 · c-009 Guardrails AI — input hardening + output validation (AD-XXX)** — New Lane-C slice on `feat/c/009-guardrails-ai` (spec `3ae1f4c` was already committed; ran straight to a working slice per the "implement + keep it minimal, production-important, no fluff, eval-first" directive). Confirmed the current (mid-2026) Guardrails AI API via a 3-agent web-research fan-out before coding (`guardrails-ai` 0.10.2; lightweight core; `import guardrails`; `Guard().use(V, on_fail=…)`; `guard.validate(text, metadata={"context": …})` for `bespoke_minicheck` — **not** `sources`; `detect_jailbreak` default threshold 0.81; `bias_check`/`toxic_language` local, `bespoke_minicheck` remote+`BESPOKE_API_KEY`; all hub validators need `guardrails configure` + install). Built `dsm/guardrails/{__init__,input_guard,output_guard,narrative_guard}.py` around a **thin duck-typed guard seam** (lazy `guardrails` import; `build_*` return `None` when disabled/uninstalled → logged no-op) so `make check` stays hermetic with guardrails-ai **not** installed. Wired at the composition root (`_guarded_score_predictor` + `_guarded_rationale_predictor` in `commands.py`; ingest enrich-loop injection check). Added `guardrails:` config, the `guardrails` optional extra + **two import-linter contracts** (`match ⊥ guardrails`, `guardrails ⊥ spine/pii/ingest/index`), a tech.md Stack line, and **AD-XXX**. **Key minimal deviations from the literal spec (all recorded in AD-XXX):** score-bounds = pure-Python clamp+log (not `valid_range` — avoids a hub-install in the commit gate; already enforced by the frozen `ScoreExtraction` validator); query-time injection → PII-safe logged skip + a new **Tier-1 eval invariant** (not a new frozen `ExclusionReason`); bias/toxicity on-fail = withhold-and-log (not DSPy `REASK`). Tests: offline guardrails unit tests (hermetic stubs) + 1 Tier-1 injection invariant (in `test_invariants.py`, gates the commit) + a grounding **PII-boundary** regression test + `tests/eval/test_guardrails_live.py` (real validators, skip without the extra). **Then addressed a 7-finding engineer review (all real):** **(#1, boundary-critical)** `bespoke_minicheck` grounding was sending de-anonymised narrative + raw gold feedback to a **remote** API (Rule 3 violation) → new `_grounded_narrative` at the composition root **redacts + leak-scans** the narrative + sources under the vault identity (+ NER) **before** the remote call and de-anonymises the filtered result (parity with the score-call boundary, AD-101; local validators unaffected — on-box like Presidio); **(#2)** near-miss rationale predictor had no input guard → added `validate_input` (a gate-excluded candidate's adversarial text reaches that LLM); **(#3)** `validate_narrative` fail-**closed** on every exception, contradicting the "fail-open" docstring → now withholds only on a `ValidationError` detection, fail-**open** on an infra error (a model crash can't blank the shortlist); **(#4)** `build_narrative_guard` discarded a working BiasCheck if ToxicLanguage failed to import → per-validator try; **(#6)** grounding reverted to the ungrounded original when FILTER stripped everything → returns the filtered output verbatim (empty if fully ungrounded); **(#7)** `validate_scores` warning is unreachable (AD-030 clamps at construction) → reframed honestly as a regression tripwire. `make check` **GREEN** (570 passed, 11 skipped, **Tier-1 20**, **7 import contracts**); `make eval` **GREEN** (58 passed, 1 skipped — `test_guardrails_live.py` skips cleanly without the extra). No frozen-contract change → no `contract-snapshot`. Next: commit + PR + adversarial review → `/handoff-index` (assigns the real AD number).
- **2026-06-29 · c-008 post-UI-feedback fixes (from running the page)** — After booting `dsm serve` and matching, three issues surfaced; diagnosed + fixed. (1) **Card overflow** — a 64-char `cid:` name shoved the score off the edge → the name now wraps (`overflow-wrap`, `flex:0 1 auto; min-width:0`), the score column is pinned, and a short label (`Candidate <8hex>`) renders on a vault miss. (2) **Names showed `cid:` not real names** — `data/identity/vault.json` didn't exist: the local `data/gold` was a **stale ingest** predating the vault wiring (a plain re-run is a no-op because landing is content-hash idempotent). Cleared the derived layers (bronze/silver/gold/identity, all rebuildable from `raw`) + re-ingested → vault repopulated → real names render. (3) **Résumé not openable** — gold `profile_pdf` citations carry `source_hash=None` in the actual data, so `/resume` 404'd. Switched résumé resolution to **silver** (`NormalizedRecord` `source_type=RESUME` → bronze `source_hash`); gold citations kept as a fallback. New `_silver_resume_hashes` + a silver-path test (and the missing-blob/confirm tests from the review). Verified end-to-end on the re-ingested data: `/resume` → 200 `application/pdf`; a full `/match/query` returns `name_is_real=True, has_resume=True`. `make check` **GREEN** (534 passed). Ops note: Milvus Lite is single-connection — overlapping `dsm serve` instances throw "Open local milvus failed"; run one.
- **2026-06-29 · c-008 web frontend (FastAPI + static page, AD-113)** — New Lane-C slice, pulled forward by request after the result-card mockup was approved ("now lets implement the frontend"). Oriented via a **7-reader understand workflow** over the spine (match entry, PII edge, output contracts, résumé-blob path, NL two-step, doc/process constraints, test harness), then wrote `specs/c-008-web-frontend/{requirements,design,tasks}.md`. Per the user's "implement" directive, ran straight through to a working slice (spec as the design record, not a hard stop). Decided to **reuse `commands._run_role` directly** (pre-guarding freshness in the web service so a `refuse` → clean 409 and `_run_role`'s `typer.Exit` branch is never reached) rather than fork the composition — zero drift, monkeypatch-compatible. Built `dsm/web/{__init__,models,service,app}.py` + `static/index.html` (the approved warm-paper/cobalt mockup CSS verbatim, JS rewritten to `fetch`) + `dsm serve` in `cli/main.py`. The match response is a **web-local view** pairing the pseudonymous `candidate_id` (captured pre-render) with the de-anonymised identity (post-render) → **frozen `dsm/models.py` untouched**, no `contract-snapshot`. Ratified **AD-XXX** (placeholder) for the web API + the `fastapi`/`uvicorn`/`python-multipart` deps + the module-placement/PII-boundary call; reworded `product.md` § Out of scope (web UI boundary moved; learning-loop still out) + added the `tech.md` Stack "Web" line + a `web:` config section. Tests in `tests/web/` mirror the CLI monkeypatch pattern (TestClient + stubbed `_build_*`, `get_paths` override to tmp dirs): app behaviours, a CLI/API **parity** test, an AST **import-boundary** test (`dsm.web ⊥ dsm.pii`). Then ran a **4-lens adversarial review (10 agents → 6 raw, all verified)**: fixed (1) `data/decisions/` not gitignored → operator free-text `reason` could be committed (added `.gitignore` rule + `.gitkeep`); (2) **XSS** — `esc()` didn't escape `'`, so a CSV-crafted `role_id` broke out of an inline `onclick` JS-string and executed (entity-escaping `'` is insufficient there because the browser HTML-decodes it before the JS parser — switched the generated handlers to `data-*` attributes + **delegated listeners**, also hardened `esc()`); (3) `/resume` raised an untyped **500** when gold cited a hash whose bronze blob was missing → catch `OSError` → typed 404 (FR-8); (4) `/match/query` ignored the documented `confirm` gate → now enforced (400 if absent); (5) `design.md` contract block had drifted from the shipped models → synced. `make check` **GREEN** (533 passed, 11 skipped, 19 Tier-1, 5 contracts — `dsm.web` adds no new forbidden import); `make eval` **GREEN** (57 passed). Branch `feat/c/008-web-frontend`; not yet committed/merged. Next: review + merge → `/handoff-index`.
- **2026-06-29 · c-007 query-side negation (A2, AD-112)** — After merging c-006 (PR #34) + the index refresh (PR #35, which assigned AD-110/AD-111), picked up **A2** (the deferred negation half of Part A). Wrote `specs/c-007-query-negation/{requirements,design,tasks}.md`, ran a **3-lens adversarial review** over the draft: confirmed **1 major** — an excluded candidate would re-surface as a near-miss (`build_near_misses` treats `LOCATION_MISMATCH` as negotiable) → skip excluded-city rows (FR-3-AC-5) — plus **2 high-conf gaps**: "anywhere but X" (no positive city) must assemble as a distributed role not a missing-location clarification (FR-4-AC-1), and the `LOCATION_MISMATCH` detail must re-check the exclusion since the gate returns a bool (FR-3-AC-6). Folded all three in; the review also **affirmed** `frozenset` is correct, the embedding never sees the exclusion, and `exclude_cities` is an LLM-parsed *fact* (not the c-006 co-location policy blocker). Implemented T-000…T-004: ratified **AD-112** (placeholder); `exclude_cities: frozenset[str]=frozenset()` on `OpenRole` + `TargetProfileScorecard` (`make contract-snapshot`); `is_excluded_city` gate helper + enforcement + near-miss skip + clarify thread-through; `RoleIntake.exclude_cities` + `assemble_role` (incl. anywhere-but-X distributed) + prompt rule + echo line; a negation golden case in the NL eval. 16 new tests. `make check` **GREEN** (512 passed, 19 Tier-1, 5 contracts; `gates.py` import-clean). Branch `feat/c/007-query-negation`. Next: review + merge → `/handoff-index`.
- **2026-06-29 · c-006 follow-up — NL-intake parse-quality eval (T-005) + impl-review fixes** — After the implementation-review fixes (FR-1-AC-2 temperature builder test; echo shows the resolved date *with its phrase*; empty-`--query` guard; both-missing + out-of-window coverage), added the **NL-intake eval** to close the one unmeasured spot (the LLM parse itself). New `tests/fixtures/nl_intake_golden.json` (6 signed-off phrasings: prose + golden `recorded_intake` + expected assembly), typed loader `dsm/eval/nl_intake_golden.py` (mirrors `golden_set.py`, reuses frozen `RoleIntake`), and `tests/eval/test_nl_intake.py` — an **offline** deterministic tier (replay golden parse → `assemble_role` → expected; 7 tests) + a **live** key-gated real-LLM tier (3 cases). Both verified green (offline deterministic; live ran once against the real LLM — all 3 parsed correctly incl. the never-guess missing-location case). Runs under `make eval`, not `make check` (eval-only, like the AI eval layer). Brought the eval in-scope in the spec (design §8 + T-005) and ticked the backlog "NL-parse Tier-2/3 cassette" item done. Pushed to PR #34. `make check` still GREEN; `make eval` NL tiers green.
- **2026-06-29 · c-006 natural-language query intake (AD-110/AD-111)** — New Lane-C slice (pulled forward by request; ahead of the c-005/AD-084 reservation, which is preserved). Oriented, wrote `specs/c-006-nl-query-intake/{requirements,design,tasks}.md`, then ran a **5-lens adversarial review workflow** (12 agents) over the draft before sign-off: it confirmed **1 blocker** — under `--yes` an LLM-emitted `co_location_required` would reach the location gate unmediated (AD-002 violation) → fixed by **deriving co-location in Python** and removing the field from `RoleIntake` (FR-8) — and **1 major** — FR-7 over-claimed freshness `warn` on the NL path → corrected to `ok`/`refuse`-only (warn structurally unreachable; NL stricter than a backdated CSV). Folded both + ~10 refinements (NL skips clarify's LLM → single interpretation; forced skill depth per bucket; determinism boundary = post-clarify scorecard; explicit temp-0 pin; unscanned-pass-through framing) into the spec; got sign-off with the recommended options (keep C-006; keep `--yes`; LLM-resolves/Python-validates dates). Implemented T-000…T-004: ratified **AD-112**/**AD-XXY** (placeholders); added `nl_intake` config + `role_intake.md` prompt + `RoleIntake` (match-local, no co-location field); `dsm/match/intake.py` (`RoleIntakeSignature` + `make_intake_predictor` + pure `assemble_role`/`resolve_start_date` + `intake_cache_key` + `IntakeCache`); CLI wiring (extract shared `_run_role`; `_match_query`; `FileIntakeCache`; `--query`/`--yes`; `--role-id` optional + exactly-one). 31 new tests (22 intake unit + 9 CLI). `make check` **GREEN** (496 passed, 11 skipped, 19 Tier-1, 5 contracts; `match ⊥ PII` holds — intake imports no `dsm.pii`). No frozen-contract change → no `contract-snapshot`. Branch `feat/c/006-nl-query-intake`. Next: review + merge → `/handoff-index` (assigns real AD numbers); then c-005 (AD-084).
- **2026-06-23 · c-004 AI eval layer (AD-104/105/106)** — Re-sequenced: evals moved to c-004 (was c-005), AD-084 embed-path scan becomes c-005. Wrote spec `specs/c-004-ai-eval-layer/{requirements,design,tasks}.md` (18 EARS requirements, 8 tasks). Ratified **AD-104** (golden set structure + draft/signed-off gating), **AD-105** (DeepEval G-Eval faithfulness judge, adopted only if TPR/TNR ≥ 0.80), **AD-106** (deterministic Recall@K + contextual precision). Implemented T-000 through T-007: typed golden set loader (`dsm/eval/golden_set.py` with `NarrativeFixture` model), deterministic retrieval metrics (`dsm/eval/retrieval_quality.py`), DeepEval G-Eval faithfulness judge routed through OpenRouter (`dsm/eval/faithfulness.py`), 21-case golden set (`tests/fixtures/golden_set.json` — 3 base roles, 13 perturbation variants, 5 faithfulness adversarials), 13 offline retrieval tests + 3 live faithfulness tests. Then ran a thorough review pass fixing 10 findings: (1) `all_labels.update()` label clobber → per-case composite keys `{case_id}:{cid}`; (2) vacuous adoption when zero predictions match → zero-total guard returns `adopted=False`; (3) `_build_judge_model()` silently ignored OpenRouter when OpenAI key also set → OpenRouter always takes priority; (4) flaky strict-inequality smoke test → 0.15 noise margin; (5) O(n) judge construction → optional `judge` kwarg for reuse; (6) internal DeepEval import path → try/except with diagnostic; (7) README keys column → lists both key options; (8) perturbation cases structurally dead → by-design for future pipeline integration; (9) inert `test_validate_skips_on_draft_labels` → removed; (10) vacuous TNR with no negatives → covered by fix #2. Then populated 58 narrative fixtures from cassettes (faithful) + crafted adversarials (unfaithful), obtained **human sign-off** on golden set labels, and ran the full end-to-end validation: **58 real LLM judge calls, TPR/TNR both ≥ 0.80, judge ADOPTED**. Raised **PR #27** (`feat/c/004-ai-eval-layer`). `make check` **GREEN** (444 unit + 19 Tier-1, 5 contracts). `make eval` GREEN (16 AI eval tests: 13 offline + 2 smoke + 1 TPR/TNR validation). Next: review + merge PR #27 → `/handoff-index`; then c-005 (AD-084 embed-path PII scan) — write spec and stop for sign-off.
- **2026-06-23 · decisions closed (post-merge interview)** — PR #25 merged to `main`; ran `/handoff-index` (index now reflects AD-001…AD-102, GREEN). Closed the three open Lane-C decisions via a human interview: **(1)** vault-miss → **warn-only** (fail-closed + spaCy startup-check declined) — ratified **AD-103**; **(2)** sequencing → **AD-084 first, then evals**; **(3)** evals → **yes, full path** (labelled golden set → *validated* DeepEval faithfulness judge → retrieval Recall@K, non-gating). Re-sequenced Next up accordingly; Blockers cleared. No code change this turn (docs only: AD-103 + lane re-plan). Next: write the **c-004 (AD-084) spec** and stop for sign-off.
- **2026-06-23 · c-003 rebase + review-fixes + PR #25** — Rebased `feat/c/003-pseudonymised-lm-boundary` onto latest `main`. **ADR collision:** B-4 merged first and claimed AD-097…AD-100 (near-miss), so c-003's PII ADRs were **renumbered AD-097/098 → AD-101/102** across code/specs/tests/config/lane (decision.md keeps both, with a renumber note); main's near-miss AD-097/098 references in `commands.py`/`models.py`/`score.py` left untouched. Only rebase conflict was `docs/decision.md` (ADR numbering). Then addressed 3 review findings on the boundary: (1) `score_candidate` no longer swallows a leak-scan trip — a `PIILeakError` (matched by class name, since `dsm.match ⊥ dsm.pii`) propagates + logs `score.pii_leak_block` instead of a generic per-candidate skip; (2) the known-PII redact pass is now **single-pass over the original text** (longest-first alternation) so a short id can't corrupt a placeholder a prior id inserted (`["Aarav Pii","Ii"]` no longer mangles `[[PII_0]]`); (3) `_pii_aware_score_predictor` logs `score.vault_miss_reduced_redaction` on a vault miss — turning the silent empty-vault + degraded-NER leak path observable (fail-closed deferred → Blockers). Also fixed a **latent bug** the warning exposed: structlog defaulted to **stdout**, corrupting the JSON `match`/`explain` print → now configured to **stderr**. Raised **PR #25** (MERGEABLE, base `main`). `make check` **GREEN** (444 unit + 19 Tier-1, 5 contracts); `make eval` GREEN. Next: review + merge PR #25 → `/handoff-index`; decide Finding-3 fail-closed.
- **2026-06-23 · c-003-pseudonymised-lm-boundary** — Implemented T-000-ADR…T-007 + review hardening. Ratified **AD-101** (real PseudonymisedLM query-time boundary) + **AD-102** (minimal persistent `FileVault` + `get_identity`; plaintext, encryption deferred to AD-068). Closed the real query-time leak: the serving `Candidate`'s `feedback`/`profile_summary` carry **de-anonymised** gold free-text, and `score` sent them through the no-op stub. Now: `PseudonymisedLM.__call__` redacts prompt+messages under one `Redactor` (deterministic vault-backed strip + NER residual), `assert_no_leak` hard-gates each fragment, forwards, then de-anonymises the response; **unset `pii_context` = pass-through** (clarify, §7). New `Redactor`/`redact_fragments` give one stable cross-fragment mapping (`dsm/pii/redact.py`). `FileVault` (`dsm/pii/vault.py`) persists `cid→(name,email)` (gitignored `data/identity/`, atomic write); ingest **writes** it (`--vault-path`, defaults beside gold), query **reads** it. Wiring lives at the CLI (`_pii_aware_score_predictor` resolves the vault by `candidate_id` + enters `pii_context`) so **`dsm.match` stays PII-free** — enforced by a **new 5th import contract** (`Match ⊥ PII`). `no-PII-leak` eval invariant upgraded from structural-only → exercises the real anonymiser (planted-name Tier-1 test + deliberately-failing fixture); Tier-3 live smoke drives the full vault→pii_context→PseudonymisedLM path. Ran an adversarial review workflow (29 agents): 7 findings confirmed, 17 refuted — fixed 5 (case-sensitive NER round-trip, `deanonymize` fixpoint for nested placeholders, non-str guard, `FileVault` type-validated load + atomic flush); the AD-084 embed-path third-party-name leak is real but deferred (Next up #3). `make check` **GREEN** (419 unit + 19 Tier-1, 5 contracts); `make eval` GREEN (Tier-1+2; Tier-3 skips without keys). Branch: `feat/c/003-pseudonymised-lm-boundary`. Next: merge + `/handoff-index`.
- **2026-06-23 · c-002-query-eval-harness** — Implemented T-000 through T-010: ratified AD-093 (three-tier eval architecture, drop promptfoo) + AD-094 (marker scheme + collection rules). Built six pure invariant evaluators in `dsm/eval/invariants.py` (gates-respected, hard-skill-not-cleared-by-adjacency, evidence-cited, no-PII-leak, determinism, adjacency-flag). Created golden-case + cassette-LM framework in `dsm/eval/cases.py` with `pseudonymise_candidates` mirroring `GoldCandidateStore`. Hand-authored cassette JSON fixtures for ROLE-01/02/03 under `tests/fixtures/cassettes/`. Enriched seed fixtures with `profile_summary` and `FeedbackEntry` items; added Suresh (java, HARD_SKILL_MISMATCH) + `desired_skills=[java/DESIRED]` to ROLE-01 for adjacency testing. Tier-1 runner: 17 tests (all six invariants × golden cases + deliberately-failing tampered fixtures + provider guard proof). Tier-2 signature regression: 7 tests (cassette freshness + clarify/score shape pins). Tier-3 live smoke + drift guard: 2 tests (skipif no keys). Wired `make eval` (no longer stubs), `make eval-tier1` (Tier-1 in `make check`), `make eval-record` (cassette regeneration). Updated eval README. `make check` GREEN (387 unit + 17 Tier-1 eval, 4 contracts). `make eval` GREEN (26 tests). Branch: `feat/c/002-query-eval-harness`. Next: merge to main, then real `PseudonymisedLM` boundary.
- **2026-06-16 · c-001-gates-rank** — Implemented T-001…T-007: real location + availability gates (`dsm/match/gates.py`; AD-020/021/022, AD-063a; shared `effective_free_date`), config-free rank sort/tie-break/top-k (`dsm/match/rank.py`; AD-043), orchestrator no-match path + `build_near_misses` (`dsm/cli/commands.py`; AD-063b/c/d, gaps recomputed from structured data not `detail`), and importable ROLE-01/02/03 fixtures (`tests/fixtures/`). Added `dsm/config.py` YAML loader + declared PyYAML, recorded as **AD-064** (orchestrator owns config + `config_snapshot`; rank stays config-free) — confirmed with the human at the post-T-003 checkpoint before adding the dep. All 24 EARS criteria covered by tests; adversarial review clean; closed the design's empty-candidate-list edge case. `make check` GREEN (66 tests, 2 import contracts; `gates.py` import-clean). Next: merge + `/handoff-index`, then wire `make eval` on the seed fixtures.
- **2026-06-14 · lane-split** — Split shared `progress.md` into per-lane files; seeded this Lane C file from the Quality/PII/Interface slices. `make check` GREEN (29 tests, 2 import contracts). Next: wire up `make eval`.
