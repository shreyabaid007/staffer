# progress.md — Live build state (index)

> **Read this first at the start of every session, then your lane file `docs/progress.<lane>.md`.** This is the shared index: global facts only (build phase, what works, active specs, decisions). Per-lane state lives in the lane files. (The rules of *how* we work live in `CLAUDE.md`.) A fresh session should be able to orient from this file alone.
> Per-lane progress goes in `docs/progress.A.md` / `.B.md` / `.C.md` — see _Lane files_ below. Section headers are stable so `/handoff-index` can target them. Keep them.

## Current status
- **Build phase:** **Query-time deterministic foundation is live.** The full ingestion pipeline (landing → bronze → silver → gold → index) is complete, and the first query-time slice has landed: the `Location` model is split into `remote_within_country` + `onsite_cities` (AD-086), a query-time freshness guard validates supply snapshot staleness (AD-087), a demand CSV parser produces typed `OpenRole`s with hard/desired skill classification, and an exact hard-skill filter drops candidates missing required skills with `HARD_SKILL_MISMATCH` exclusions (AD-088). BGE embedder + cross-encoder reranker remain deployed to Modal serverless GPU (T4) behind the typed `EmbedClient` protocol (AD-074/AD-071/AD-080). **Hybrid recall (dense + BM25 + RRF) + rerank + the full `dsm match` serving pipeline are still not wired**; `dsm/index/stub.py` still backs `dsm match`. `PseudonymisedLM` anonymise/deanonymise still the Slice-0 pass-through stub. Contracts frozen with signed-off amendments — AD-075/076/077/086/088.
- **Active slice:** Last slice merged to `main` is the query-time deterministic foundation (PR #18, `feat/b/001-query-deterministic`). The c-001 gates+rank implementation remains **deprecated** (AD-085 — code stays in the tree but the design is provisional). The draft query-time architecture (`ee-query-architecture.md`) is the companion to `ee-ingestion-architecture.md`. Next: ratify the full query architecture, then wire hybrid recall + rerank + scoring into `dsm match`. The team works **sequentially by phase** (ingestion → indexing → query architecture → …).
- **Harness (`make check`):** GREEN — format, lint, typecheck, 340 tests (11 skipped — opt-in real-data + Modal smoke), 3 import contracts all pass.
- **`main`:** Slice 0 foundation (PR #3) + per-lane progress files (PR #4); index refresh (PR #6); real gates/rank/no-match (PR #5, `feat/c/001-gates-rank` — **now deprecated, AD-085**); ingestion architecture docs + AD-065…AD-074 (PR #9); ingestion landing+parse → bronze (PR #10, `feat/a/001-ingest-landing-parse`); ingestion silver + identity vault seed (PR #11, `feat/a/002-ingest-silver-identity`); ingestion enrich → canonical gold (PR #12, `spec/a/003-ingest-enrich-gold`) + re-run tombstone fix (PR #13); Modal embedder + reranker deployment (PR #14, `feat/a/004-modal-embed-rerank`); index build + upsert to Milvus Lite (PR #15, `feat/a/005-index-upsert`); c-001 gates/rank deprecated + draft `ee-query-architecture.md` added (AD-085); **query-time deterministic foundation: Location split, freshness guard, demand parser, exact hard-skill filter (PR #18, `feat/b/001-query-deterministic`, AD-086/087/088)**.

## Works end-to-end right now
- `uv run dsm match --role-id ROLE-STUB-01` — runs the full pipeline (real gates → stub retrieve/score → real rank, or real no-match) over stub ingest and prints a valid `ShortlistResult` / `NoMatchResult` JSON. **The gates/rank/no-match implementation (c-001) is deprecated (AD-085)** — it still runs green, but the design is provisional and will be redone against the ratified query-time architecture.
- `uv run dsm ingest` — lands raw files (`data/raw` → bronze), parses (CSV/PDF/markdown) → bronze, normalizes → silver `NormalizedRecord`s, then **enriches resumes/feedback through the PII boundary (redact → leak-scan → DSPy extract @ temp 0 → de-anonymize → verify-quote) and merges silver+enriched into one canonical `GoldCandidate` per `candidate_id`** (§7 authority, resume↔feedback conflicts recorded never averaged), reconciles against prior gold (tombstones departed; an idempotent re-run is a no-op for gold), writes `gold/<cid>.json` (+`gold_hash`), and prints PII-safe `── Silver ──` + `── Gold ──` summaries; exits 1 on errors / leak-block. `make smoke` runs an opt-in real-data smoke.
- `uv run dsm index` — builds the **write-time** index from gold: projects each `GoldCandidate` into a PII-free `CandidateIndexRecord` (filter fields from `Sourced[...].value`), builds a deterministic capability-only `embed_text` + a `skill_set` that **excludes feedback-denied skills** (`demonstrated is False`, AD-081), embeds the passage via the injected `EmbedClient` (`ModalEmbedClient` in prod, `FakeEmbedClient` in tests), and upserts into the Milvus Lite `candidates` collection (`data/index/milvus.db`: PK `candidate_id`, dense `FLOAT_VECTOR` dim 768 / metric IP, `skill_set` `ARRAY<VARCHAR>` + BM25 `SPARSE_FLOAT_VECTOR`). Re-embeds + upserts only when stored `(gold_hash, model_version)` doesn't match (AD-082); tombstones delete, thin profiles (missing grade/location/availability) are skipped. Prints a PII-safe `── Index ──` summary (indexed / skipped-unchanged / tombstoned-removed / thin-skipped). `embed_text`/`skill_set` are PII-free **by construction** — no outbound known-PII scan at index time (AD-084). Index schema now carries `remote_within_country: bool` + `onsite_cities: ARRAY<VARCHAR>` (AD-086). ([`dsm/index/indexer.py`](../dsm/index/indexer.py), [`milvus_store.py`](../dsm/index/milvus_store.py), [`models.py`](../dsm/index/models.py))
- **Query-time deterministic foundation** (PR #18) — four new modules, all LLM-free and deterministic:
  - [`dsm/match/freshness.py`](../dsm/match/freshness.py) — `check_freshness()` validates supply snapshot staleness against demand `as_of` date (AD-087). Three-way verdict: ok / warn / refuse.
  - [`dsm/match/demand.py`](../dsm/match/demand.py) — `parse_demand(csv_path)` parses an Open Roles CSV into typed `OpenRole`s: banner `demand_as_of` extraction, skill split (`expert|advanced|intermediate|beginner` → HARD, `nice to have` or bare → DESIRED), `co_location_required`, `Priority` batch ordering. Malformed rows logged + skipped.
  - [`dsm/index/retrieve.py`](../dsm/index/retrieve.py) — `exact_hard_skill_filter(pool, hard_skills)` filters candidates by `skill_set` membership + `ProficiencyLevel` ordinal floor. Excluded candidates get `Exclusion(reason=HARD_SKILL_MISMATCH)` (AD-088).
  - [`dsm/match/models.py`](../dsm/match/models.py) — query-time intermediates: `OpenRolesBanner`, `DemandParseOutcome`.
- **Modal embedder + reranker** ([`modal/embedder.py`](../modal/embedder.py)) — `BAAI/bge-base-en-v1.5` (768-dim, L2-normalized, asymmetric passage/query) + `BAAI/bge-reranker-base` (cross-encoder) deployed to Modal serverless T4 GPU as `staffer-models` app. Weights baked into the container image (zero-download cold start). `DSM_MODAL_SMOKE=1 uv run pytest tests/index/test_modal_smoke.py` verifies the live deployment. Typed [`EmbedClient`](../dsm/index/embed_client.py) protocol + `ModalEmbedClient` implementation in `dsm/index/embed_client.py` (AD-074/AD-071/AD-080).
- **Reusable PII layer** ([`dsm/pii/`](../dsm/pii/)) — generic `redact.py` (deterministic known-PII strip first + Presidio NER seam + de-anonymize), `leakscan.py` (outbound hard gate, fails the build on residual PII), and the `vault.py` `candidate_id = HMAC(email)` derivation; ingest reaches the LLM only through `PseudonymisedLM` (AD-078). Encrypted at-rest vault + NER/org-dictionary hardening are Lane C, later.
- **Deterministic gates** ([`dsm/match/gates.py`](../dsm/match/gates.py)) — location gate rewritten to AD-086 semantics: onsite (city match OR `onsite_cities` membership, case-insensitive; `remote_within_country` never clears) + distributed (same country). Availability gate unchanged (AD-021/022). LLM-free, import-clean. _Deprecated (AD-085): runs green, design provisional pending the query-time architecture._
- **Ranking** ([`dsm/match/rank.py`](../dsm/match/rank.py)) — deterministic sort/tie-break/top-k; verified to match §6.10 sort order; config-free (orchestrator owns config via [`dsm/config.py`](../dsm/config.py), AD-064). _Deprecated (AD-085): runs green, design provisional._
- **No-match path + near-misses** ([`dsm/cli/commands.py`](../dsm/cli/commands.py)) — orchestrator builds `NoMatchResult` with ordered, capped near-misses (AD-063b/c/d); near-miss ordering updated to include `HARD_SKILL_MISMATCH` (below availability misses, AD-088). _Deprecated (AD-085): runs green, design provisional._
- `dsm/models.py` — frozen Pydantic v2 domain contracts (AD-060), with `Location.city` optional (AD-075), `Location.remote_eligible` split into `remote_within_country` + `onsite_cities` (AD-086), `ExclusionReason.HARD_SKILL_MISMATCH` added (AD-088), and `EvidenceCitation` carrying optional `source_hash`/`locator` as the verified-quote type (AD-077).
- `make check` — 340 tests green (11 skipped — opt-in real-data + Modal smoke), 0 type errors, 3 import contracts (gates ⊥ PII/index/LLM; no direct LLM provider access; ingest ⊥ match/index — ingest may now use the `dsm.pii` boundary + `dspy`, the sanctioned LLM path, AD-078). `dsm/index/` tests run against a `FakeEmbedClient` + a temp in-process Milvus Lite db (zero network, NF-1). Importable ROLE-01/02/03 seed fixtures in `tests/fixtures/` (reusable by `dsm/eval/`).

## Lane files
Per-lane In flight / Next up / Blockers / Session log live in these append-only files. Read the index, then your lane file.
- [`docs/progress.A.md`](progress.A.md) — **Lane A: Data & Retrieval** (Eng A — ingest, index, gates, retrieval).
- [`docs/progress.B.md`](progress.B.md) — **Lane B: Reasoning** (Eng B — clarify, score, rank).
- [`docs/progress.C.md`](progress.C.md) — **Lane C: Quality, PII & Interface** (Eng C — PII boundary, CLI, eval/quality).

## Active specs
- `specs/000-foundation/` — complete, approved, merged to `main`.
- `specs/c-001-gates-rank/` — implemented + merged to `main` (PR #5), but **deprecated (AD-085)**: the gates/rank/no-match code stays in the tree, yet its design (and AD-020…023 / 030…035 / 040…043 / 063) is **provisional** and will be redone against the ratified query-time architecture.
- `specs/a-001-ingest-landing-parse/` — complete, approved, merged to `main` (PR #10). First ingestion slice: landing + parse → bronze.
- `specs/a-002-ingest-silver-identity/` — complete, approved, merged to `main` (PR #11). Second ingestion slice: bronze → silver normalization + identity vault seed (AD-075/076).
- `specs/a-003-ingest-enrich-gold/` — complete, approved, merged to `main` (PR #12, + fix PR #13). Third ingestion slice: LLM enrich (PII-bracketed, cited) + canonical merge → gold + reconcile/tombstones (AD-077/078/079).
- `specs/a-004-modal-embed-rerank/` — complete, approved, merged to `main` (PR #14). Modal deployment of BGE embedder + reranker + typed `EmbedClient` protocol (AD-080).
- `specs/a-005-index-upsert/` — complete, approved, merged to `main` (PR #15). Write-time retrieval: gold → PII-free `CandidateIndexRecord`, embed (passage) → upsert to Milvus Lite `candidates` (dense IP + BM25 sparse), `(gold_hash, model_version)` re-embed gating, `dsm index` CLI (AD-081/082/083/084).
- `specs/b-001-query-deterministic/` — complete, approved, merged to `main` (PR #18). Query-time deterministic foundation: `Location` split into `remote_within_country` + `onsite_cities` (AD-086), query-time freshness guard (AD-087), `HARD_SKILL_MISMATCH` exclusion reason (AD-088), demand CSV parser, exact hard-skill filter, location gate rewrite to AD-086 semantics.
- _Signed-off design (not a spec):_ [`ee-ingestion-architecture.md`](../ee-ingestion-architecture.md) — full ingestion subsystem, accepted as AD-065…AD-074. a-001/a-002/a-003 implement its landing+parse, silver, and enrich+gold stages; a-004 deploys the embedding/reranking models to Modal; a-005 implements its §6 Phase 6 + §8 write-time index/embed.
- _Partially ratified design:_ [`ee-query-architecture.md`](../ee-query-architecture.md) — the **query-time (serving)** subsystem, companion to ee-ingestion: the five-stage spine `gate pre-filter → exact hard-skill filter → hybrid recall → rerank → score + combine`. **b-001 ratified three ADRs (AD-086/087/088)** covering the deterministic foundation (Location model, freshness, hard-skill filter); the remaining query-time ADRs (hybrid recall, rerank wiring, score+combine) **still require team sign-off before implementation**. This is the doc that will resettle the deprecated c-001 gating/ranking/scoring decisions (AD-085).
- _Next (planned):_ Ratify the remaining `ee-query-architecture.md` sections (hybrid recall, rerank, score+combine), then a query-time retrieval spec — replace `dsm/index/stub.py` with hybrid dense + BM25 + RRF recall over the Milvus Lite collection, `mode="query"` embed, `rerank()` wiring into `dsm match`, and the redone score/rank per the ratified design. Still pending: the real `PseudonymisedLM` provider/anonymiser, the generic outbound NER + client-org-dictionary scan (the AD-084 seam on `build_embed_text`), and the encrypted identity-vault write path. No spec written yet.

## Decisions
- Authoritative log: `docs/decision.md` (current range AD-001 … AD-088; next starts at AD-089). Most recent (b-001): **AD-086** (split `Location.remote_eligible` into `remote_within_country` + `onsite_cities` — frozen-contract amendment; supersedes AD-063a/AD-020), **AD-087** (query-time freshness guard: ok/warn/refuse based on demand vs supply `as_of` staleness), **AD-088** (add `ExclusionReason.HARD_SKILL_MISMATCH` — frozen-contract amendment; near-miss ordering: hard-skill-gap below availability). Prior: **AD-085** (c-001 deprecated; AD-020…023/030…035/040…043/063 provisional). Prior (a-005): **AD-081/082/083/084**. Prior (a-004): **AD-080**. Prior (a-003): **AD-077/078/079**; (a-002): **AD-075/076**; ingestion architecture: **AD-065…AD-074**.
- **Freeze the contracts after Slice 0.** Churn breaks parallel lane work — change only via team agreement + a new ADR. b-001 amended the contracts (AD-086/088) with sign-off.

---

## Session log — pre-split archive (frozen)
Shared history from before the per-lane split (2026-06-14). **Frozen — do not append here.** New entries go in the lane files' session logs.
- **2026-06-11 · slice-0-foundation** — Completed tasks F-005 through F-016: stub ingest/gates/clarify/score/rank/index/pii modules, CLI `dsm match`, `config/default.yaml`, eval scaffold, import-linter passing, 29 tests green. `make check` fully green. Slice 0 done. Next: merge to main, begin Slice 1 (real gates).
- **2026-06-11 · slice-0-models** — Implemented tasks F-003 + F-004: wrote `dsm/models.py` (19 Pydantic v2 frozen models, all enums as `StrEnum`) and `tests/test_models.py` (27 tests — instantiation, discriminated union, validation rejection). `make check` green. Added AD-060 (contracts frozen). Also fixed `pyproject.toml` import-linter config (`include_external_packages = true`). Next: F-005 stub ingest through F-016.
- **2026-06-11 · slice-0-spec** — Wrote and approved `specs/000-foundation/` (requirements, design, tasks). 16 tasks (F-001 to F-016) defined for frozen contracts + stubbed CLI + green harness. Committed to branch `spec/000-foundation`. No implementation yet. Next: execute tasks.
- **2026-06-08 · setup** — Created the operating-system layer (`CLAUDE`, `product`, `tech`, `structure`, `decision`) and this file. No code. Next: Slice 0 foundation.

---

## Maintaining this file
This index describes `main`. It is refreshed **only at merge to `main`**, by whoever merges, via `/handoff-index` — which rewrites the global sections (_Current status_, _Works end-to-end_, _Active specs_, _Decisions_). See `.claude/commands/handoff-index.md`. While working on a feature branch, do **not** edit this file — update only your own lane file via `/handoff` (lane resolved from `.claude/lane`; see `.claude/commands/handoff.md`).
