# progress.md — Live build state (index)

> **Read this first at the start of every session, then your lane file `docs/progress.<lane>.md`.** This is the shared index: global facts only (build phase, what works, active specs, decisions). Per-lane state lives in the lane files. (The rules of *how* we work live in `CLAUDE.md`.) A fresh session should be able to orient from this file alone.
> Per-lane progress goes in `docs/progress.A.md` / `.B.md` / `.C.md` — see _Lane files_ below. Section headers are stable so `/handoff-index` can target them. Keep them.

## Current status
- **Build phase:** **Query-time serving is live end-to-end and the real PII boundary is in place.** The full ingestion pipeline (landing → bronze → silver → gold → index) is complete and patched. The query pipeline runs the full 9-step spine: deterministic gates, exact hard-skill filter (AD-088), hybrid dense + BM25 + RRF recall (AD-089, **ON by default** — `index.recall.enabled=true`), cross-encoder rerank, DSPy clarify over `PseudonymisedLM` with echo fallback, LLM sub-scores with deterministic combine + adjacency map + cited flags, and freshness wiring. **`PseudonymisedLM` is now the real outbound anonymiser at query time (AD-101)** — no longer a pass-through stub: per call it redacts prompt+messages (deterministic vault-backed strip + NER residual), leak-scans each fragment as a hard gate, forwards, then de-anonymises the response; **unset `pii_context` = pass-through** (clarify). A persistent, gitignored **`FileVault`** (AD-102) holds `candidate_id → (name,email)` (ingest writes, query reads); encryption deferred (AD-068). No-match results now carry **near-miss skill verdicts + `closest_on_skills`** (b-004, AD-099/100) with optional LLM `selection_rationale` (AD-098). Contracts frozen with signed-off amendments — AD-075/076/077/086/088/091/092/100/102.
- **Active slice:** Most recent merge to `main`: **faithfulness judge calibration fix (PR #28)** — graded 1–10 rubric replacing binary criteria, 10 subtle adversarials added to golden set (now 31 cases / 95 labels: 80 faithful + 15 adversarial), threshold 0.10→0.70, TPR 1.00 / TNR 1.00, fully demoable. Prior: c-004 AI eval layer (PR #27, AD-104/105/106), c-003 real PseudonymisedLM boundary (PR #25, AD-101/102), b-004 near-miss skill verdict + `closest_on_skills` (PR #24, AD-097…100), c-002 three-tier eval harness (PR #23, AD-095/096). The c-001 gates+rank implementation remains **deprecated** (AD-085). Next planned: AD-084 generic outbound NER/org scan on the index/embed path, AD-068 encrypted/retention-hardened vault.
- **Harness (`make check`):** **GREEN** — 444 passed, 11 skipped, **19 Tier-1 eval** (cassette, no network), **5 import contracts** all pass (gates ⊥ PII/index/LLM; no direct LLM provider access; ingest ⊥ match/index; match/index ⊥ ingest; **match ⊥ PII** — AD-101). `make eval` GREEN (Tier-1+2+3; **45 tests** — 14 retrieval-quality deterministic + 2 faithfulness smoke + 1 judge validation `eval_live` + 19 Tier-1 invariant + 3 signature/cassette + 3 live-smoke + 3 cassette-freshness; judge validation `eval_live` runs with signed-off golden set + API keys, reports TPR/TNR via `warnings.warn`).
- **`main`:** Slice 0 foundation (PR #3) + per-lane progress files (PR #4); index refresh (PR #6); real gates/rank/no-match (PR #5, `feat/c/001-gates-rank` — **now deprecated, AD-085**); ingestion architecture docs + AD-065…AD-074 (PR #9); ingestion landing+parse → bronze (PR #10, `feat/a/001-ingest-landing-parse`); ingestion silver + identity vault seed (PR #11, `feat/a/002-ingest-silver-identity`); ingestion enrich → canonical gold (PR #12, `spec/a/003-ingest-enrich-gold`) + re-run tombstone fix (PR #13); Modal embedder + reranker deployment (PR #14, `feat/a/004-modal-embed-rerank`); index build + upsert to Milvus Lite (PR #15, `feat/a/005-index-upsert`); c-001 gates/rank deprecated + draft `ee-query-architecture.md` added (AD-085); query-time deterministic foundation (PR #18, `feat/b/001-query-deterministic`, AD-086/087/088); query-time scoring + orchestration (PR #20, `feat/b/002-query-scoring-orchestration`, AD-089/090/091/092); Milvus index fix (PR #21); ingestion reconcile + revive bug fix (PR #22, AD-093/094); recall flipped ON by default; **three-tier eval harness (PR #23, `feat/c/002-query-eval-harness`, AD-095/096); near-miss skill verdict + `closest_on_skills` (PR #24, `feat/b/004-near-miss-skill-verdict`, AD-097/098/099/100); real PseudonymisedLM PII boundary + `FileVault` (PR #25, `feat/c/003-pseudonymised-lm-boundary`, AD-101/102); Lane-C decisions close-out (PR #26, AD-103); AI eval layer (PR #27, `feat/c/004-ai-eval-layer`, AD-104/105/106); faithfulness judge calibration fix (PR #28, `fix/c-004-faithfulness-judge-calibration`)**.

## Works end-to-end right now
- `uv run dsm match --role-id <ID>` — runs the **full 9-step query pipeline** for a single role parsed from the demand CSV: (1) load config, (2) parse demand CSV → typed `OpenRole`, (3) check freshness (AD-087), (4) load index + gate pre-filter (location AD-086 + availability), (5) exact hard-skill filter (AD-088), (6) hybrid recall (dense + BM25 + RRF — passthrough when `index.recall.enabled=false`, AD-089), (7) cross-encoder rerank with top-k truncation, (8) hydrate serving `Candidate`s via `CandidateStore` (AD-091), (9) DSPy clarify → LLM sub-scores (skill match + availability + seniority + feedback) → deterministic combine + adjacency map + cited flags → rank → `ShortlistResult` / `NoMatchResult` JSON. Freshness warnings surface as `FRESHNESS_WARNING` flags (AD-092). Seniority is a soft signal, never a gate (AD-090). LLM sub-scores clamped to [0,1] before combine.
- `uv run dsm explain --role-id <ID>` — re-runs the same pipeline and dumps full lineage per §9: role parse, freshness verdict, gate/filter exclusions, recall/rerank trace, per-candidate score breakdown, and final ranking.
- `uv run dsm ingest` — lands raw files (`data/raw` → bronze), parses (CSV/PDF/markdown) → bronze, normalizes → silver `NormalizedRecord`s, then **enriches resumes/feedback through the PII boundary (redact → leak-scan → DSPy extract @ temp 0 → de-anonymize → verify-quote) and merges silver+enriched into one canonical `GoldCandidate` per `candidate_id`** (§7 authority, resume↔feedback conflicts recorded never averaged), reconciles against prior gold (tombstones departed; an idempotent re-run is a no-op for gold), writes `gold/<cid>.json` (+`gold_hash`), and prints PII-safe `── Silver ──` + `── Gold ──` summaries; exits 1 on errors / leak-block. `make smoke` runs an opt-in real-data smoke.
- `uv run dsm index` — builds the **write-time** index from gold: projects each `GoldCandidate` into a PII-free `CandidateIndexRecord` (filter fields from `Sourced[...].value`), builds a deterministic capability-only `embed_text` + a `skill_set` that **excludes feedback-denied skills** (`demonstrated is False`, AD-081), embeds the passage via the injected `EmbedClient` (`ModalEmbedClient` in prod, `FakeEmbedClient` in tests), and upserts into the Milvus Lite `candidates` collection (`data/index/milvus.db`: PK `candidate_id`, dense `FLOAT_VECTOR` dim 768 / metric IP, `skill_set` `ARRAY<VARCHAR>` + BM25 `SPARSE_FLOAT_VECTOR`). Re-embeds + upserts only when stored `(gold_hash, model_version)` doesn't match (AD-082); tombstones delete, thin profiles (missing grade/location/availability) are skipped. Prints a PII-safe `── Index ──` summary (indexed / skipped-unchanged / tombstoned-removed / thin-skipped). `embed_text`/`skill_set` are PII-free **by construction** — no outbound known-PII scan at index time (AD-084). Index schema carries `remote_within_country: bool` + `onsite_cities: ARRAY<VARCHAR>` (AD-086).
- **Query-time modules** (b-001 deterministic foundation + b-002 scoring/orchestration):
  - [`dsm/match/freshness.py`](../dsm/match/freshness.py) — `check_freshness()`: supply snapshot staleness vs demand `as_of` → ok / warn / refuse (AD-087).
  - [`dsm/match/demand.py`](../dsm/match/demand.py) — `parse_demand(csv_path)` → typed `OpenRole`s with hard/desired skill classification, `co_location_required`, `Priority` batch ordering.
  - [`dsm/index/retrieve.py`](../dsm/index/retrieve.py) — `exact_hard_skill_filter()` (AD-088), `hybrid_recall()` (dense + BM25 + RRF, AD-089), `rerank()` (cross-encoder top-k truncation + error fallback).
  - [`dsm/index/text_builder.py`](../dsm/index/text_builder.py) — symmetric role query passage builder for rerank (§6.6/§6.7).
  - [`dsm/match/clarify.py`](../dsm/match/clarify.py) — bounded DSPy clarify over `PseudonymisedLM` with echo fallback (§6.2).
  - [`dsm/match/score.py`](../dsm/match/score.py) — LLM sub-scores (skill match, availability, seniority, feedback) + deterministic combine + adjacency map + cited flags (§6.8). Scores clamped to [0,1].
  - [`dsm/match/gates.py`](../dsm/match/gates.py) — location gate (AD-086 semantics) + availability gate (AD-021/022). LLM-free, import-clean.
  - [`dsm/match/rank.py`](../dsm/match/rank.py) — deterministic sort/tie-break/top-k (§6.10 sort order).
  - [`dsm/index/build.py`](../dsm/index/build.py) — build edge: `is_indexable`/`project_filter_fields`/`build_record` (AD-091, exempt from the match/index ⊥ ingest contract).
  - [`dsm/match/models.py`](../dsm/match/models.py) — query-time intermediates: `OpenRolesBanner`, `DemandParseOutcome`.
  - Orchestrator lives in [`dsm/cli/commands.py`](../dsm/cli/commands.py) (the composition root): wires `GoldCandidateStore` adapter → full pipeline → JSON output.
- **`CandidateStore` port** (AD-091) — `CandidateStore` protocol in `dsm/models.py`; `GoldCandidateStore` adapter in `dsm/cli/commands.py` hydrates serving `Candidate`s from gold. `match`/`index` import the interface only; the adapter is injected at the CLI. Serving `Candidate.email`/`name` carry the pseudonymised `candidate_id`; the real name/email is resolved from the `FileVault` (AD-102) **only at the query-time redaction wiring** (and is never put in output — output stays pseudonymised this slice).
- **Modal embedder + reranker** ([`modal/embedder.py`](../modal/embedder.py)) — `BAAI/bge-base-en-v1.5` (768-dim, L2-normalized, asymmetric passage/query) + `BAAI/bge-reranker-base` (cross-encoder) deployed to Modal serverless T4 GPU as `staffer-models` app. Rerank `top_k` set to 10 in config. `DSM_MODAL_SMOKE=1 uv run pytest tests/index/test_modal_smoke.py` verifies the live deployment. Typed [`EmbedClient`](../dsm/index/embed_client.py) protocol + `ModalEmbedClient` implementation (AD-074/AD-071/AD-080).
- **Real PII boundary** ([`dsm/pii/`](../dsm/pii/)) — `PseudonymisedLM` (AD-101) is the real outbound anonymiser at **both** LLM call sites (ingest `enrich` + query `score`): per call, when a `pii_context(known_pii)` is active it redacts the prompt + every message under one stable `Redactor` (`redact.py` — deterministic known-PII strip first + Presidio NER residual, cross-fragment-stable placeholders), `leakscan.assert_no_leak` hard-gates each outbound fragment (a leak-scan trip **propagates**, never a silent skip), forwards, then de-anonymises the response. **Unset context = pass-through** (clarify, role text only). `vault.py` adds **`FileVault`** — a persistent, gitignored, atomically-written `candidate_id → (name,email)` store with `get_identity` (AD-102); `candidate_id = HMAC(email)` derivation unchanged. Query-time wiring lives at the CLI (`_pii_aware_score_predictor` resolves identity from the vault + enters `pii_context`), so **`dsm.match` imports no `dsm.pii`** (enforced by the `match ⊥ PII` contract). Logs route to stderr so the JSON `match`/`explain` print stays clean. **Deferred:** at-rest encryption/retention/purge (AD-068); a generic outbound NER/org scan on the index/embed path (AD-084 — third-party names in gold `projects` can still reach Modal unredacted).
- `dsm/models.py` — frozen Pydantic v2 domain contracts (AD-060), with `Location.city` optional (AD-075), `Location.remote_eligible` split into `remote_within_country` + `onsite_cities` (AD-086), `ExclusionReason.HARD_SKILL_MISMATCH` (AD-088), `FlagType.FRESHNESS_WARNING` (AD-092), `CandidateStore` protocol (AD-091), `Grade` enum (moved from ingest, AD-091), and `EvidenceCitation` carrying optional `source_hash`/`locator` as the verified-quote type (AD-077).
- **No-match explainability** (b-004) — a `NoMatchResult` carries curated **near-misses** (candidates one *negotiable* gate from eligibility — availability/location — that **clear** the hard skills, AD-099) and a disjoint **`closest_on_skills`** list (cleared both gates, only a hard-skill short, AD-100), each optionally annotated with an LLM `selection_rationale` (AD-098, via the PII-safe predictor). Skill gaps are computed by the shared `hard_skill_gap` helper (no adjacency, AD-033/072).
- **AI eval layer** (c-004, AD-104/105/106) — model-quality metrics, `make eval` only (never `make check`). **Golden set** (`tests/fixtures/golden_set.json`, AD-104): 21 hand-labelled cases (base roles + perturbation variants + adversarial narratives), typed loader in `dsm/eval/golden_set.py`, `review_status: "signed_off"`. **Retrieval metrics** (`dsm/eval/retrieval_quality.py`, AD-106): deterministic Recall@K + contextual precision, 14 unit tests. **Faithfulness judge** (`dsm/eval/faithfulness.py`, AD-105): DeepEval G-Eval over OpenRouter (`anthropic/claude-sonnet-4-6`), `build_faithfulness_judge()` + `judge_narrative()` + `validate_judge()`, 2 smoke tests (`eval_live`), TPR/TNR validation with optimal-threshold search against signed-off golden labels. **Calibrated (PR #28):** graded 1–10 rubric (4 bands: faithful/minor/embellishment/fabrication), evidence-grounded narratives, 95 labels (80 faithful + 15 adversarial — 5 blatant + 10 subtle), threshold 0.70, TPR 1.00 / TNR 1.00 (adopted). Validation report displayed via `warnings.warn`.
- `make check` — **444 passed, 11 skipped, 0 type errors**, 19 Tier-1 eval, **5 import contracts** (gates ⊥ PII/index/LLM; no direct LLM provider access; ingest ⊥ match/index; match/index ⊥ ingest — AD-091; **match ⊥ PII** — AD-101). `make eval` GREEN (Tier-1+2+3; 16 AI eval tests — `eval_live` faithfulness judge skips without keys). `dsm/index/` tests run against a `FakeEmbedClient` + a temp in-process Milvus Lite db (zero network, NF-1). PII tests use a fake inner LM + fake NER (no network). Importable ROLE-01/02/03 seed fixtures + cassettes in `tests/fixtures/`.

## Lane files
Per-lane In flight / Next up / Blockers / Session log live in these append-only files. Read the index, then your lane file.
- [`docs/progress.A.md`](progress.A.md) — **Lane A: Data & Retrieval** (Eng A — ingest, index, gates, retrieval).
- [`docs/progress.B.md`](progress.B.md) — **Lane B: Reasoning** (Eng B — clarify, score, rank).
- [`docs/progress.C.md`](progress.C.md) — **Lane C: Quality, PII & Interface** (Eng C — PII boundary, CLI, eval/quality).

## Active specs
- `specs/000-foundation/` — complete, approved, merged to `main`.
- `specs/c-001-gates-rank/` — implemented + merged to `main` (PR #5), but **deprecated (AD-085)**: the gates/rank/no-match code stays in the tree, yet its design (and AD-020…023 / 030…035 / 040…043 / 063) is **provisional** — superseded by the b-002 orchestrator and the ratified query-time architecture.
- `specs/a-001-ingest-landing-parse/` — complete, approved, merged to `main` (PR #10). First ingestion slice: landing + parse → bronze.
- `specs/a-002-ingest-silver-identity/` — complete, approved, merged to `main` (PR #11). Second ingestion slice: bronze → silver normalization + identity vault seed (AD-075/076).
- `specs/a-003-ingest-enrich-gold/` — complete, approved, merged to `main` (PR #12, + fix PR #13). Third ingestion slice: LLM enrich (PII-bracketed, cited) + canonical merge → gold + reconcile/tombstones (AD-077/078/079).
- `specs/a-004-modal-embed-rerank/` — complete, approved, merged to `main` (PR #14). Modal deployment of BGE embedder + reranker + typed `EmbedClient` protocol (AD-080).
- `specs/a-005-index-upsert/` — complete, approved, merged to `main` (PR #15). Write-time retrieval: gold → PII-free `CandidateIndexRecord`, embed (passage) → upsert to Milvus Lite `candidates` (dense IP + BM25 sparse), `(gold_hash, model_version)` re-embed gating, `dsm index` CLI (AD-081/082/083/084).
- `specs/b-001-query-deterministic/` — complete, approved, merged to `main` (PR #18). Query-time deterministic foundation: `Location` split (AD-086), freshness guard (AD-087), hard-skill filter + exclusion reason (AD-088), demand CSV parser, location gate rewrite.
- `specs/b-002-query-scoring-orchestration/` — complete, approved, merged to `main` (PR #20). Query-time scoring + retrieval + orchestration: hybrid recall (AD-089), seniority soft-signal (AD-090), `CandidateStore` port + `Grade` move + match/index ⊥ ingest contract (AD-091), `FlagType.FRESHNESS_WARNING` (AD-092), DSPy clarify, LLM sub-scores + combine, cross-encoder rerank, 9-step orchestrator, `dsm explain` CLI.
- _Signed-off design (not a spec):_ [`ee-ingestion-architecture.md`](../ee-ingestion-architecture.md) — full ingestion subsystem, accepted as AD-065…AD-074. a-001/a-002/a-003 implement its landing+parse, silver, and enrich+gold stages; a-004 deploys the embedding/reranking models to Modal; a-005 implements its §6 Phase 6 + §8 write-time index/embed.
- _Substantially implemented design:_ [`ee-query-architecture.md`](../ee-query-architecture.md) — the **query-time (serving)** subsystem, companion to ee-ingestion: the five-stage spine `gate pre-filter → exact hard-skill filter → hybrid recall → rerank → score + combine`. **b-001 ratified AD-086/087/088** (deterministic foundation); **b-002 ratified AD-089/090/091/092** (hybrid recall, seniority, CandidateStore, freshness flag) and implemented the full pipeline end-to-end. The deprecated c-001 gating/ranking/scoring decisions (AD-085) are now superseded by b-002's orchestrator.
- `specs/c-002-query-eval-harness/` — complete, approved, merged to `main` (PR #23). Three-tier pytest eval harness: Tier 1 (six deterministic invariant evaluators — gates-respected, hard-skill-not-cleared-by-adjacency, evidence-cited, no-PII-leak, determinism, adjacency-flag), Tier 2 (signature regression + cassette freshness), Tier 3 (live smoke + cassette drift guard). Cassette LM + golden cases ROLE-01/02/03. `make eval` wired. AD-095/096.
- `specs/b-004-near-miss-skill-verdict/` — complete, approved, merged to `main` (PR #24). No-match enrichment: near-misses must clear hard skills (AD-099), a disjoint `closest_on_skills` list on `NoMatchResult` (AD-100, frozen-contract amendment), optional LLM `selection_rationale` (AD-098), shared `hard_skill_gap` helper. (Renumber note: AD-097 was superseded by AD-099 within this slice.)
- `specs/c-003-pseudonymised-lm-boundary/` — complete, approved, merged to `main` (PR #25). Real `PseudonymisedLM` query-time boundary (AD-101) + minimal persistent `FileVault` + `get_identity` (AD-102): `pii_context` call-context, redact/leak-scan/de-anonymise over prompt+messages, CLI-level wiring keeps `dsm.match` PII-free (`match ⊥ PII` contract), `no-PII-leak` invariant upgraded to exercise the real anonymiser. (ADRs renumbered from AD-097/098 at rebase — b-004 claimed those numbers first.)
- `specs/c-004-ai-eval-layer/` — complete, approved, merged to `main` (PR #27 + calibration fix PR #28). AI eval layer: typed golden set loader + 31 labelled cases / 95 labels with signed-off labels (AD-104), DeepEval G-Eval faithfulness judge via OpenRouter with optimal-threshold validation (AD-105), deterministic retrieval metrics Recall@K + contextual precision (AD-106). PR #28 calibrated the judge: graded 1–10 rubric, evidence-grounded narratives, 15 adversarials (5 blatant + 10 subtle), threshold 0.70, TPR 1.00 / TNR 1.00. 45 eval tests total; judge TPR/TNR validation runs when keys present + labels signed off.
- _Next (planned):_ generic outbound NER/org scan on the index/embed path (AD-084), encrypted/retention-hardened identity vault (AD-068). No spec written yet.

## Decisions
- Authoritative log: `docs/decision.md` (current range AD-001 … AD-106; next starts at AD-107). Most recent (c-004 AI eval layer, PR #27): **AD-104** (typed golden set — 21 labelled cases, `review_status` sign-off gate, per-case composite keys), **AD-105** (DeepEval G-Eval faithfulness judge via OpenRouter, optimal-threshold adoption, TPR/TNR >= 0.80), **AD-106** (deterministic Recall@K + contextual precision retrieval metrics). Prior (c-003 PII boundary, PR #25): **AD-101** (real `PseudonymisedLM` query-time boundary), **AD-102** (minimal persistent `FileVault` + `get_identity`; encryption deferred). Prior Lane-C decisions (PR #26): **AD-103** (vault-miss warn-only). Prior (b-004 near-miss, PR #24): **AD-099** (near-miss must clear hard skills), **AD-100** (`NoMatchResult.closest_on_skills`), **AD-098** (LLM `selection_rationale`), **AD-097** (superseded by AD-099). Prior (c-002 eval, PR #23): **AD-095/096**. Prior (PR #22): **AD-093/094**. Prior (b-002): **AD-089/090/091/092**; (b-001): **AD-086/087/088**; **AD-085** (c-001 deprecated); (a-005): **AD-081…084**; (a-004): **AD-080**; (a-003): **AD-077/078/079**; (a-002): **AD-075/076**; ingestion architecture: **AD-065…AD-074**.
- **Freeze the contracts after Slice 0.** Churn breaks parallel lane work — change only via team agreement + a new ADR. Signed-off amendments to date: b-001 (AD-086/088), b-002 (AD-091/092), b-004 (AD-100), c-003 (AD-102).
- **ADR numbering note:** c-003 was authored as AD-097/098 but renumbered to **AD-101/102** at rebase, because b-004 merged first and claimed AD-097…AD-100. The registry is collision-free.

---

## Session log — pre-split archive (frozen)
Shared history from before the per-lane split (2026-06-14). **Frozen — do not append here.** New entries go in the lane files' session logs.
- **2026-06-11 · slice-0-foundation** — Completed tasks F-005 through F-016: stub ingest/gates/clarify/score/rank/index/pii modules, CLI `dsm match`, `config/default.yaml`, eval scaffold, import-linter passing, 29 tests green. `make check` fully green. Slice 0 done. Next: merge to main, begin Slice 1 (real gates).
- **2026-06-11 · slice-0-models** — Implemented tasks F-003 + F-004: wrote `dsm/models.py` (19 Pydantic v2 frozen models, all enums as `StrEnum`) and `tests/test_models.py` (27 tests — instantiation, discriminated union, validation rejection). `make check` green. Added AD-060 (contracts frozen). Also fixed `pyproject.toml` import-linter config (`include_external_packages = true`). Next: F-005 stub ingest through F-016.
- **2026-06-11 · slice-0-spec** — Wrote and approved `specs/000-foundation/` (requirements, design, tasks). 16 tasks (F-001 to F-016) defined for frozen contracts + stubbed CLI + green harness. Committed to branch `spec/000-foundation`. No implementation yet. Next: execute tasks.
- **2026-06-08 · setup** — Created the operating-system layer (`CLAUDE`, `product`, `tech`, `structure`, `decision`) and this file. No code. Next: Slice 0 foundation.

---

## Maintaining this file
This index describes `main`. It is refreshed **only at merge to `main`**, by whoever merges, via `/handoff-index` — which rewrites the global sections (_Current status_, _Works end-to-end_, _Active specs_, _Decisions_). See `.claude/commands/handoff-index.md`. While working on a feature branch, do **not** edit this file — update only your own lane file via `/handoff` (lane resolved from `.claude/lane`; see `.claude/commands/handoff.md`).
