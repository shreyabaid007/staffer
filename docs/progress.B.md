# progress.B.md — Lane B: Reasoning

> Lane file. Owner: **Eng B — Reasoning** (clarify, score, rank, reasoning).
> Global facts (build phase, contracts, decisions) live in the index `docs/progress.md` — don't duplicate them here.
> Headers below are stable so `/handoff` can target them. Append to the session log, newest first; rewrite the other sections from current state.

## In flight
- _No active Lane-B slice._ All Lane-B work is **merged to `main`**: B-1 deterministic foundation (PR #18, AD-086/087/088), B-2 scoring + orchestration (PR #20, AD-089/090/091/092), **B-4 near-miss skill verdict + `closest_on_skills` (PR #24, AD-097…100)**, and **B-3 human-readable shortlist (PR #29, AD-107)** — `render_identities(result, vault)` substitutes real `(name, email)` from the `FileVault` into **all** identity fields (ranked assessments, both `NearMiss` lists, full exclusion log) at the CLI output edge, closing the AD-091/AD-102 deferral with no pipeline/logic change and no eval-invariant relaxation. Read the index `docs/progress.md` for the live build state.

## Next up
1. **Tune the recall trip point** — hybrid recall is now **ON by default** (`index.recall.enabled: true`, ratified **AD-109**; was the deferred-OFF AD-089). `top_n` (=100) bounds the dense/BM25/RRF fan-out — tune it against real gated-pool sizes (§12 #6); set `enabled: false` to force the exhaustive path on tiny pools.
2. **Distributed-gate detail wording** — the country-mismatch `LOCATION_MISMATCH` `detail` in `filter_candidates` still reuses co-location phrasing (dormant: all data is India). Tidy when the distributed gate gets real exercise.

## Blockers / needs a human
- _(none active — B-1…B-4 all merged.)_
- **`dsm match` / `dsm explain` need live deps to run for real** — a built Milvus index + Modal (embed/rerank) + OpenRouter + gold on disk + a demand CSV (`data/raw/demand/open_roles.csv`, now present). `make check` covers the orchestration with all seams mocked (no network, NF-1); a real end-to-end run is a separate live/eval concern (Lane-C eval path, §12 #9). `PseudonymisedLM` is now the **real** anonymiser (AD-101, PR #25) — no longer the Slice-0 stub; both LLM call sites (score + the near-miss `selection_rationale`, built at the CLI edge) route through it, and stay `None`/echo on the pure-unit path with no predictor injected. Final human-facing identity is de-anonymised at the CLI output edge (B-3, AD-107).

## Session log (append-only — newest first)
- **2026-06-24 · b-003-human-readable-shortlist** — Implemented B-3 (T-000…T-005) on `feat/b/003-human-readable-shortlist`. Spec written + signed off (`specs/b-003-human-readable-shortlist/`). Ratified **AD-107** (final human-facing identity de-anonymisation at the CLI output edge) — closes the deferred "final human-facing identity rendering (vault read)" noted in AD-091/AD-102 and the B-2/B-4 blockers. **Problem:** shortlist/no-match output carried the pseudonymised `candidate_id` in `Candidate.email`/`name`, `NearMiss.candidate_email`/`name`, and `Exclusion.candidate_email` (AD-091) — everything else was already real gold data. **Fix (output-only, no logic change):** new `render_identities(result, vault)` in `dsm/cli/commands.py` resolves each `candidate_id` → real `(name, email)` from the `FileVault` (AD-102, already built + read at the CLI) and rebuilds the frozen output models via `model_copy`, covering **all** identity fields incl. the full exclusion log (user decisions: *JSON with real identity* + *everything incl. exclusion log*). `_match_role` now returns `(result, vault)`; `match`/`explain` apply the render before output. `run_match`/`_match_role` keep emitting **pseudonymised** results → determinism invariant + eval cassettes untouched; de-anon lives only at the CLI → `dsm.match` gains no `dsm.pii` import (`match ⊥ PII` contract green); the `no-PII-leak` invariant needs **no relaxation** (governs provider inputs + narratives, not output identity — verified in `dsm/eval/invariants.py::no_pii_leak`). Vault-miss → keep `candidate_id` + PII-safe `WARNING` (`render.vault_miss_identity`, AD-103). **Storage:** no new store — identity already lives in `data/identity/vault.json` (plaintext, AD-102); spec records that output exposure makes AD-068 hardening more pressing (still out of scope). **No new dependencies.** Tests: `tests/cli/test_render_identities.py` (5 unit — AC-1…AC-4 + purity, vault-miss asserted via `structlog.testing.capture_logs`) + a seeded-vault CLI smoke in `test_orchestrator.py`. `make check` **GREEN** — 450 passed, 11 skipped, 5 import contracts. Next: merge B-3 to `main` (+ `/handoff-index`); B-4 still unmerged.
- **2026-06-23 · b-004 · AD-100 closest-on-skills (Part 3)** — Added a second no-match section after the ROLE-05 question ("why zero near-misses?" — AD-099 correctly drops skill-failers, but then a skills-short no-match shows nothing). **AD-100** (additive frozen amendment): `NoMatchResult.closest_on_skills: list[NearMiss]` — candidates who cleared **both** gates and failed only the hard-skill filter (`HARD_SKILL_MISMATCH`), ranked by fewest hard-skill gaps (missing + below-floor) then email, capped 3, reusing the AD-098 rationale. The mirror of a near-miss (near = clears skills + one negotiable gap; closest = clears gates + a skill short); the two are disjoint (one exclusion reason per candidate). Refactored `dsm/index/retrieve.py` to expose a structured `hard_skill_gap` helper (+ `HardSkillGap` model) reused by both `exact_hard_skill_filter`'s detail string and the new builder (one source of truth; exclusion `detail` wording unchanged). New `build_closest_on_skills` in commands.py; `_no_match` builds + caps + rationale-annotates both lists; `_lineage`/JSON carry the section. Folded into the b-004 spec as Part 3 (b-005 dropped). Verified on real gold: ROLE-05 (all-hard no-match) → 21 closest pre-cap, top-3 led by a candidate missing only Spring Boot. `make check` **GREEN** — 413 passed, 11 skipped, 4 contracts.
- **2026-06-23 · b-004 · AD-099 refinement** — Reframed near-misses after review ("why surface a candidate a date shift won't fix?"). **AD-099** (supersedes AD-088 + AD-097): a near-miss must be *one fixable decision* from eligibility — fails only a negotiable gate (availability/location) **and** clears the non-negotiable hard skills. `build_near_misses` now keeps only availability/location misses that pass `exact_hard_skill_filter`; pure `HARD_SKILL_MISMATCH` misses **and** gate misses that also fail a skill are dropped from near-misses (still in the `exclusion_log` — the transparency layer). Reverted the AD-097 skill suffix + 4-tuple sub-ordering: `gap_summary` is plain negotiable-gap wording again, ordering back to AD-063b. AD-098 (`selection_rationale`) unaffected — still annotates the surviving near-misses. Updated decision.md (AD-099, AD-097 marked superseded, AD-088 near-miss note), the b-004 spec (FR-1/2/5/6/7 restated), and tests (`test_no_match` AD-099 cases + reverted ROLE-03 wording; `test_orchestrator` hard-skill case → empty near-misses + exclusion-log assertion). `make check` **GREEN** — 403 passed, 11 skipped, 4 contracts.
- **2026-06-23 · b-004-near-miss-skill-verdict** — Implemented B-4 (T-000…T-008) on `feat/b/004-near-miss-skill-verdict`, 6 commits. Spec written + signed off (`specs/b-004-near-miss-skill-verdict/`). Ratified **AD-097** (near-misses report their hard-skill verdict — no model change) and **AD-098** (`NearMiss.selection_rationale`, an additive frozen-contract amendment AD-060). **Problem:** a no-match near-miss labelled "available 1 day after deadline" hid whether the candidate would clear skills — availability/location misses are excluded *at the gate*, before `exact_hard_skill_filter` runs, so their skills were never checked. **Part 1 (deterministic):** `build_near_misses` now reruns the pre-skill-filter misses through `exact_hard_skill_filter` (no adjacency AD-033, floor AD-072 — verdict can't drift from the real gate) and appends the verdict to `gap_summary` (`"; clears all hard skills"` / `"; also missing N hard skill(s): …"` / below-floor wording); sort keys widened to a uniform `(type_rank, sub_rank, metric, email)` 4-tuple so skill-clearing availability misses (actionable) rank above those with a remaining gap. `Exclusion.detail` still never parsed (AD-063c). **Part 2 (LLM):** added optional `NearMiss.selection_rationale`; new `NearMissRationalePredictor` seam + `make_near_miss_rationale_predictor` (DSPy over `PseudonymisedLM`, PII-free inputs — skills/feedback/gap only) + `explain_near_misses` applier (skip-on-error → `None`) in `dsm/match/score.py`; `config/prompts/near_miss_rationale.md`. `run_match`/`_no_match` thread an optional `near_miss_predict`; rationale generated **only for the shown top-3** (after the AD-063d cap); live predictor built at the CLI edge. Eligibility stays deterministic + LLM-free; determinism invariant binds set+order+`gap_summary`, not the prose. **No new dependencies.** `make check` **GREEN** — 403 passed, 11 skipped, 4 import contracts (incl. no-direct-LLM / no-PII). Next: merge B-4 to `main` (+ `/handoff-index`); enable recall when pools grow; demand CSV fixture; delete dead stubs.
- **2026-06-21 · b-002-query-scoring-orchestration** — Implemented B-2 (T-000…T-011) on `feat/b/002-query-scoring-orchestration`, 13 commits. Spec written + signed off (`specs/b-002-query-scoring-orchestration/`). Ratified **AD-089** (defer hybrid recall behind `index.recall.enabled=false`), **AD-090** (seniority a soft signal, never a gate), **AD-091** (`CandidateStore` port in `dsm/models.py` + `GoldCandidateStore` adapter in `dsm/cli/store.py`; `Grade` moved to `dsm/models.py` re-exported from ingest; index models made ingest-free with projection helpers relocated to new `dsm/index/build.py`; new `match`/`index` ⊥ `ingest` import contract), and **AD-092** (add `FlagType.FRESHNESS_WARNING`, frozen amendment, signed off mid-slice). Built: `build_role_query_passage` (symmetric to candidate `build_embed_text` via shared `_skill_phrase`); `hybrid_recall` (dense⊕BM25⊕RRF, OFF→passthrough, error→exhaustive) + `rerank` (cross-encoder, top_k truncation, error→unranked) in `dsm/index/retrieve.py` + read helpers on `MilvusIndexStore` (`search_dense`/`search_bm25`/`fetch_embed_texts`); `clarify_role` bounded DSPy path over injected `predict` seam w/ echo fallback; `score_candidate` rewrite (LLM sub-scores → Python `0.7·skill+0.3·feedback` combine, exact `hard_skill_coverage` (no adjacency), adjacency-partial-credit `desired_skill_coverage` + `ADJACENCY_USED`, citation verbatim-verify drop AD-073, flags incl. freshness, LLM-error→skip); `GoldCandidateStore` hydration (excludes `demonstrated is False`, `email`/`name`=pseudonymised `candidate_id`, None proficiency→BEGINNER, source from availability); full 9-step `run_match` (gate→exact→recall→rerank→score→rank, empty-pool no-match at each narrowing stage, `build_near_misses` extended for `HARD_SKILL_MISMATCH`, extended `config_snapshot`); `match` rewrite (parse→freshness refuse/warn→clarify→run_match) + new `dsm explain` lineage dump; config recall/rerank keys + AD-035 `adjacency_map` seed + clarify/score prompts. Tooling: added `pyrightconfig.json` (pyright 1.1.410 can't parse `pyproject.toml` — pre-existing, fails at HEAD too) in its own commit. Removed obsolete Slice-0 subprocess `tests/test_cli.py`. **No new dependencies.** `make check` **GREEN** — 385 passed, 11 skipped, 4 import contracts. Next: merge B-2 to `main` (+ `/handoff-index`); enable recall when pools grow; demand CSV fixture; delete dead stub modules.
- **2026-06-21 · b-001-query-deterministic** — Implemented B-1 (T-001…T-011) on `feat/b/001-query-deterministic`, 7 commits. Ratified + applied **AD-086** (split `Location.remote_eligible` → `remote_within_country` + `onsite_cities`; onsite gate = city-match or onsite-city membership, `remote_within_country` never clears; distributed gate = same-country), **AD-087** (query-time freshness guard `check_freshness` → ok/warn/refuse), **AD-088** (`ExclusionReason.HARD_SKILL_MISMATCH`). The Location rename was done as **one atomic commit** across all consumers (models, silver `parse_location`, index `CandidateIndexRecord`/`FilterFields`/projections, Milvus schema `BOOL`+`ARRAY<VARCHAR>`, fixtures, near-miss wording, gates) to keep `make check` green — a frozen-field rename can't stay green per-task. New modules: `dsm/match/models.py` (`OpenRolesBanner`/`DemandParseOutcome`), `dsm/match/freshness.py`, `dsm/match/demand.py` (`parse_demand`: banner→`demand_as_of`, skill encodings, co-location, Notes→description, Priority order, log+skip malformed, missing-banner blocks), `dsm/index/retrieve.py` (`exact_hard_skill_filter`: set-membership + proficiency floor, no adjacency). `rank.py` verified unchanged vs §6.10 (docstring repointed). `make check` **GREEN** — 340 passed, 11 skipped, 3 import contracts. Corrected `.claude/lane` C→B (was mislabelled). Next: B-2 scoring + orchestrator wiring; merge B-1 to `main`.
- **2026-06-14 · lane-split** — Split shared `progress.md` into per-lane files; seeded this Lane B file from the Reasoning slices. `make check` GREEN (29 tests, 2 import contracts). Next: real reasoning after Slice 1.
