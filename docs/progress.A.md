# progress.A.md — Lane A: Data & Retrieval

> Lane file. Owner: **Eng A — Data & Retrieval** (ingest, index, gates, retrieval).
> Global facts (build phase, contracts, decisions) live in the index `docs/progress.md` — don't duplicate them here.
> Headers below are stable so `/handoff` can target them. Append to the session log, newest first; rewrite the other sections from current state.

## In flight
- _No active Lane-A slice._ Lane A's last shipped work — **`a-005-index-upsert`** (write-time index → Milvus Lite `candidates`: dense IP + BM25 sparse, `(gold_hash, model_version)` re-embed gate AD-082, AD-081/083/084) — is **merged to `main` (PR #15)**. The query-time retrieve + rerank Lane A had pencilled in as "a-006" was instead delivered by **Lane B in b-002** (`hybrid_recall` + `rerank` in `dsm/index/retrieve.py`, PR #20), and the Slice-0 `dsm/index/stub.py` was **deleted in PR #26**. Lane ownership is now advisory (AD-062); read the index `docs/progress.md` for the live build state.
- **a-001 … a-005 all merged to `main`** — a-001 (PR #10), a-002 (PR #11), a-003 (PR #12 + #13 tombstone fix), a-004 (PR #14), a-005 (PR #15).

## Next up
1. **c-005 — AD-084 generic outbound NER/org-dictionary scan** (the index/embed-path PII hardening Lane A flagged at a-005). `dsm/index/text_builder.build_embed_text` is the single attach point: a generic outbound scan there catches third-party / client-org names in gold `projects` before they reach Modal, without needing per-candidate identity. Now owned as **Lane C's c-005** (`docs/progress.C.md` Next-up #1) — coordinate so it lands once; **spec-first** (golden rule 1).
2. **Chain `dsm index` onto `dsm ingest`** — standalone `dsm index` is delivered (a-005, `dsm/cli/commands.py::index`); auto-running the index phase at the end of `ingest` is still a small follow-on (`ingest` does not invoke `index_gold` today).
3. **Live enrich validation** (carried) — run `dsm ingest` end-to-end with a live provider. **Partially unblocked:** `PseudonymisedLM` is no longer the Slice-0 stub — it's the real anonymiser since **AD-101** (PR #25); what remains is a real OpenRouter key + an end-to-end pass over real data.

## Blockers / needs a human
- _(none active.)_
- Tracked residual: the **AD-084 embed-path leak** — third-party / client-org names in gold `projects` can still reach Modal's embedder unredacted (`build_embed_text` takes no `known_pii`) — stays open until **c-005** lands. Don't describe the index path as fully PII-clean before then (mirrors Lane C's note).

## Session log (append-only — newest first)
- **2026-06-20 · a-005-index-upsert** — Implemented Slice 5 **write-time index** (spec signed off; T-000-ADR…T-007, 7 task commits + the spec commit). Added **AD-081** (`skill_set`/`embed_text` exclude `demonstrated is False` via one shared predicate so an exact/`ARRAY_CONTAINS`/BM25 hard-skill match never credits a refuted skill), **AD-082** (no embed cache — each Milvus record stores `gold_hash` **and** `model_version` = `config models.embedder`; re-embed only when either differs, so an embedder swap that `gold_hash` would miss still forces re-embed), **AD-083** (add the `milvus-lite` embedded engine — `pymilvus` is only the client; probed offline incl. BM25 `Function`+`SPARSE_FLOAT_VECTOR`+`enable_analyzer`), **AD-084** (no index-time known-PII scan — `embed_text` PII-free **by construction**; supersedes the brief's `assert_no_leak`; generic outbound NER/org-dictionary scan deferred to Lane C, `build_embed_text` is the seam). New modules: `dsm/index/models.py` (`CandidateIndexRecord` frozen + `is_indexable`/`project_filter_fields`/`build_record` + `IndexMetrics`; dates → INT64 ordinals in the store for a-006 range filters), `text_builder.py` (`included_skills`/`build_embed_text`/`build_skill_set` — pure, deterministic, sorted, reads only `skills`/`domains`/`projects`), `milvus_store.py` (`MilvusIndexStore` ensure/upsert/delete/`fetch_versions`; `candidates` collection, dense 768 IP + BM25 sparse auto-computed from `skill_text`, config-free like rank AD-064), `indexer.py` (`index_gold`: read→tombstone-delete→thin-skip→`(gold_hash,model_version)` gate→embed(passage)→upsert; injected `EmbedClient`, no `dsm.pii`). `dsm index` CLI (PII-safe `── Index ──` summary: indexed/skipped-unchanged/tombstoned-removed/thin-skipped; `_build_embed_client` seam monkeypatched in tests). Config `index.milvus` block; `data/index/*` gitignored (+`.gitkeep`); `milvus-lite>=3.0` added to `pyproject.toml`+`uv.lock`. Tests: `FakeEmbedClient` (no network) + tmp Milvus Lite, in-process — `make check` makes zero network calls. `make check` **GREEN** (291 passed, 11 skipped, 3 contracts — unchanged; no new contract). Next: PR + merge `a-005`, then a-006 query-time retrieve+rerank (BM25 sparse already built → query logic only, no re-index).
- **2026-06-19 · a-003-ingest-enrich-gold** — Implemented Slice 3 enrich + canonical merge (spec signed off; T-000-ADR…T-013, 14 task commits). Added **AD-077** (relax frozen `EvidenceCitation` with optional `source_hash`/`locator`; `text` = AD-073 verified quote — backwards-compatible), **AD-078** (relax ingest imports to the `dsm.pii` boundary + `dspy`, the sanctioned LLM path; new **generic** `redact.py`/`leakscan.py` Lane A-seeded, Lane C hardens; supersedes AD-076 narrowing — contracts 4→3), **AD-079** (feedback *facts* in gold, feedback *score* deferred to match/score — supersedes ee-ingestion §6 gold field). New modules: `dsm/pii/redact.py` (deterministic known-PII pass first, then mockable Presidio NER seam, + `deanonymize`), `dsm/pii/leakscan.py` (`leak_scan`/`assert_no_leak`/`PIILeakError` hard gate); ingest `enrich.py` (DSPy `ProfileExtraction`/`FeedbackItemExtraction` signatures via `PseudonymisedLM` temp 0; anonymize→gate→extract→de-anon→**verify every citation quote verbatim**, drop+log+count fabricated quotes, skip schema-invalid; injectable `predict` seam → cassette tests, no live LLM in `make check`), `merge.py` (§7 authority: grade/loc/avail latest supply; names union; proficiency resume>CSV; demonstrated feedback>resume; **MG-5 conflict** = both citations + recorded, never averaged; `gold_content_hash`), `goldstore.py` (`gold/<cid>.json` atomic write + `gold_hash` re-export + `list_gold_ids`), `reconcile.py` (tombstone departed + freshness guard, `today` injected). Extended `lineage.py` (RunMetrics + `build_quality_metrics`: coverage thin/medium/rich, conflicts, citation-verify failures, leak-blocks invariant, tombstones) and `Phase-4/5` models (`Sourced[T]` PEP-695 generic, `SkillExtraction`, `ProfileSummaryExtraction`, `FeedbackExtraction`, `MergedSkill`, `GoldCandidate`). `config/prompts/` versioned instructions + `enrich`/`reconcile` config. `dsm ingest` now runs enrich→merge→reconcile→gold with a PII-safe `── Gold ──` summary (supply-only → thin gold, no LLM; leak block → exit 1); `data/gold/*` gitignored (GS-3). `make check` **GREEN** (240 passed, 6 skipped, 3 contracts). Next: PR + merge `a-003`, then live-enrich validation + index/embeddings.
- **2026-06-19 · a-002 env-key wiring** — Made the `DSM_CANDIDATE_ID_KEY` runtime requirement ergonomic (`dsm ingest` fails fast without it): documented it in `.env.example`, added `[env] _.file = ".env"` to `.mise.toml` so a gitignored `.env` auto-loads, and created a local `.env` with a generated key (untracked). Confirmed the key reaches `uv run` via an interactive shell (mise activation already present in `~/.zshrc`); for bare `uv run dsm ingest`, reload the shell (`exec zsh`). One commit (`5d3e145`). `make check` **GREEN** (175 passed, 6 skipped, 4 contracts). Next: PR + merge `a-002`, then enrich/gold.
- **2026-06-19 · a-002-ingest-silver-identity** — Implemented Slice 2 silver + identity (spec signed off; T-000-ADR…T-009, 14 commits). Added AD-075 (relax frozen `Location.city → str | None`; fixed `gates.py` None-safe city compare) and AD-076 (`candidate_id = HMAC(email)` + `Vault` protocol in `dsm/pii/vault.py`, Lane A seeded; `NF-IMPORT-1` narrowed so ingest imports `dsm.pii.vault` only). New: ingest-local silver models (`Grade`/`Confidence`/`NormalizedSkill`/`NormalizedRecord`, reusing frozen `Location`/`AvailabilityState`/`ProficiencyLevel`); `taxonomy.py` + `config/taxonomy.yaml` (canonical skill map + unmapped queue); `silver.py` (per-sheet availability beach→FreeNow / rolling_off→RollingOff{date,confidence} / new_joiners→NewJoiner{join_date}; new-joiner skills `unverified` per AD-032; grade/location/date coercion; `Chennai-open` + `Location` are **two columns** → `remote_eligible` + warning; `Remote (India)` → `city=None`; log+skip+count on coercion failure; `normalize`/`normalize_run` deterministic with `valid_as_of` threading); silver-layer persistence `write_normalized` → `data/silver/records/<hash>.jsonl` (gitignored, SW-2); `dsm ingest` runs silver + prints a PII-safe `── Silver ──` summary (no `raw_text`/email to stdout; fails fast without `DSM_CANDIDATE_ID_KEY`). Verified real supply schema against `data/raw/supply/*.csv` (corrected an earlier wrong assumption that `Chennai-open` was a location value). `make check` **GREEN** (175 passed, 6 skipped, 4 contracts). Next: PR + merge, then enrich/gold (incl. vault write path, Lane C).
- **2026-06-18 · a-001 real-data validation + fixes** — Validated the bronze pipeline against real sample files (3 supply CSVs, 3 resumes, 2 feedback) — found + fixed two misalignments synthetic fixtures missed: (1) `classify` now matches supply filenames case/separator-insensitively (real `Beach.csv`/`Rolling Off.csv`/`New Joiners.csv`); (2) banner detection now handles a mid-line `as of <date>` in a full title row with `(synthetic)` suffix + CSV padding. PDF (real Docling: clean text, 11 sections, email) and Markdown (email-key fallback) confirmed working unchanged. Added an **opt-in real-data smoke test** (`make smoke`, `DSM_REAL_SMOKE=1`; skips by default — files gitignored + real Docling) + a synthetic real-shape CSV fixture. Spec/design refreshed. End-to-end on real data: 8 landed, 0 invalid, 42 records. Drafted the PR description. `make check` **GREEN** (115 passed, 6 smoke skipped, 3 contracts). ⚠️ Follow-up for a later slice: real Docling downloads/loads model weights → runtime ingest not strictly offline; needs a model pre-provisioning decision when the enrich path is wired. Next: merge, then silver stage.
- **2026-06-18 · a-001-ingest-landing-parse** — Implemented Slice 1 bronze foundation (T-001…T-010): `data/` layout + gitignored PII-dense layers; bronze models (`SourceType`/`LandingStatus`/`ManifestEntry`/`BronzeRecord`/`RunManifest`); content-addressed `LocalFSBlobStore` (atomic temp+rename); `JSONLManifest` (append-only commit marker); `land.py` (discover · classify · sha256 · dedup · blob-before-manifest crash recovery); CSV parser (banner/as-of, quoting, verbatim rows, log+skip); PDF parser (Docling → sections + `email_found` + OCR fallback, behind a mockable `_extract` seam for offline tests); markdown feedback parser (email-keyed items); `lineage.py` seed (structlog `log_invalid` + `build_run_manifest`); new import-linter contract isolating `dsm.ingest`; e2e land+parse test. Added `structlog` dep (already in tech.md §14). `make check` **GREEN** (112 tests, 3 contracts). Scope: CSV+PDF+MD bronze parsing all landed now — supersedes the old "sheets-only in Slice 1" note (scope sequencing, not an ADR). Deferred to later slices: silver/normalize/`candidate_id`/taxonomy/enrich/gold/PII redaction/embedding/reconcile. Next: merge, then the silver stage.
- **2026-06-14 · lane-split** — Split shared `progress.md` into per-lane files; seeded this Lane A file from the Data & Retrieval slices. `make check` GREEN (29 tests, 2 import contracts). Next: Slice 1 real gates.
